\# Case study notes

Running list of moments, decisions, and findings to draw on when writing the case study.

- 2026-09-02 — Codebook promised in the dataset readme was never deposited; built ground truth from the researchers' final report plus my own manual synthesis instead. R&D has gaps; show how one was handled.
- 2026-09-02 — Setup friction (git identity, pasted commands, curly quotes, pager) — first data point on what "adoptable by a design team" actually costs. I am the first user.
- 2026-09-03 — Manual synthesis: 2h50 for 5 transcripts; 9 of 11 insights matched the researchers' full-corpus findings. Baseline and validation in one.
- 2026-09-03 — Filenames lost their "Dataset-2_" prefix between download and repo; script had to be made forgiving. Product requirement discovered by hitting it.
- 2026-09-03 — Turn ID as the unit of citation: makes "unsupported insight" a mechanical check, not a judgement.
- 2026-09-03 — Loop stop conditions: critic pass, no progress, max iterations. "No progress" is the interesting one — without it the loop burns iterations on failures it can't fix.
- 2026-09-03 — Critic rules split into deterministic (code) and model-judged. Anything checkable mechanically should be: free, instant, can't hallucinate.
- 2026-09-03 — Architecture: core/ (reusable) vs synth/ (tool). Framework-plus-tools is the story, and the ETOT product.
- 2026-09-03 — First real run: critic spent its entire 8K output budget thinking, returned no verdict, and the fallback treated "no failures" as a pass. The loop approved a synthesis nobody had checked. Fix: critic silence is a hard fail, retry once, budget raised to 32K. Lesson: a checker's absence must never read as approval.
- 2026-09-03 — Run one's I-02 cited the interviewer's summary of Penni's barriers as evidence of Penni's view. Added a deterministic rule: evidence must be the participant's own turns. A rule discovered from an output, not designed in advance.
- 2026-09-03 — Raising the critic's budget hit an SDK guardrail: requests that could exceed 10 minutes must stream. Fixed in one place (core/llm.py) because every agent goes through the same client — the architecture paid for itself on day two.
- 2026-09-03 — Run three: loop converged 9 → 4 → 2 failures over three rounds; both survivors were real mis-citations. Confidence and structure errors fix in one round; citation accuracy does not — the reviser swaps a wrong turn for another wrong turn. Fixing by substitution without verification.
- 2026-09-03 — Design response: citations with receipts. Evidence = turn ID + verbatim excerpt; a deterministic check confirms the excerpt exists in the turn. Makes the dominant failure partly mechanical.
- 2026-09-03 — Logger bug: check and revise for the same iteration overwrote each other. Every call was still saved separately, which is why nothing was lost. Redundant logging earned its keep.
- 2026-09-03 — Receipts implemented as rule quote_mismatch. The first thing to watch in run four: does the synthesis agent's own accuracy improve just because it knows the check exists? If deterministic failures drop to near zero on round one, the receipt is doing its work before the critic runs.
- 2026-09-03 — Run four: receipts took deterministic failures on round one from 3 to 0, at the cost of ~3× synthesis output tokens. Critic caught a quote lifted from a turn whose overall meaning contradicts the claim. Receipts stop fabrication; only a reader stops selective quotation. Each layer has a job.
- 2026-09-03 — "Overconfident" was whack-a-mole because confidence was never defined. Now numeric in config (high = 4+ sources, no counter-evidence). The critic and the synthesiser were arguing over a threshold neither could see.
- 2026-09-03 — Four runs, zero critic passes. Decision: show the unresolved objections on the affected insights rather than hide them. A synthesis that admits where its reviewer disagreed is worth more than one that pretends consensus.
- 2026-09-03 — Tool frozen as v1-eval. Stop tuning; measure.
- 2026-09-03 — First eval matrix: two of nine runs invalid. Synthesis hit the 32K output cap twice (receipts + thinking), returned truncated JSON, zero insights — and in C-eval1 the critic passed the empty list in five seconds. The loop approved nothing. Same bug class as run one, one layer up: silence must never read as approval, at every layer.
- 2026-09-03 — C-eval3 deterministic failures never converged (11 → 5 → 6). Every persistent one was "source listed without cited turns": the model counted counter-evidence participants as sources, my rule didn't. Three rounds arguing about a definition. Fix: compute sources in code and stop asking. A rule the fixer can't satisfy is a bug in the rule; anything you can compute, don't ask the model for.
- 2026-09-03 — Model-judged catches in the same run were exactly right: a finding titled "agency-based researchers" citing an academic; "never encountered guidance" overstating what was said. The critic earns its cost on overreach.
- 2026-09-03 — Re-tagged v1.1-eval. A and B2/B3 stand (unaffected by the changes); B1 and all three C runs re-run under v1.1.
- 2026-09-03 — Eval 1 scored blind, 9 reports. The loop halved unsupported citations, zeroed confidence miscalibration, cut trap failures 2.3 → 1.3 — and dropped theme coverage from 75% to 57%. Reuse absent in every C run, present in 5 of 6 others. The critic checks what's on the page; the reviser's cheapest fix is deletion; nothing watches for what went missing. Precision bought with recall.
- 2026-09-03 — Intake alone (B) changed nothing measurable over single-prompt. The map isn't the value; the checking is.
- 2026-09-03 — v2 named by the data: a recall check from the intake topic maps, and "fix or downgrade, never delete" in the revise prompt.
- 2026-09-03 — Case study framing: a null result on coverage is the most credible thing in this project. It shows the eval could have said no.
- 2026-09-03 — v2's first three runs crashed at the critic: a code edit hadn't applied, and the offline test never exercised the real critic path. ~$1.50 of intake and synthesis wasted. Fixed by testing the full path with a stubbed model call. Cheap tests that skip the expensive path are how expensive paths fail.
- 2026-09-03 — Drafted the case study's hero card and trace from memory; checked both against the run logs before publishing. Both were wrong: the hero was a different insight, and the trace took one round, not two. Replaced with the record. The page about receipts had to earn its own.- 2026-09-03 — Packaged: README, one-command CLI, pip install, MIT + data attribution. Fifteen-minute install verified on myself as the first stranger.
- 2026-09-03 — First full-corpus run (15 transcripts, 101k words, one command): 30 min, $4.53, 20 insights, 12 → 7 → 4. Whole corpus in one synthesis call. 14/16 ground-truth themes, including four only reachable beyond my five transcripts. Six findings not in the ground truth at all. Copyright/ownership missed; deletion-as-waste missed for the third time — david:0022 cited yet again, extracted yet again as something else.
- 2026-09-03 — Manual: 170 min for 5 transcripts, extrapolated ~9 hours for 15. Loop: 30 min and $4.53 for 15. Both need a human to read the receipts.- 2026-09-03 — Eval 2: v2's recall check brought coverage from 51% to 69% (A = 75%) with unsupported citations unchanged at less than half of A's. Reuse 0/3 → 3/3. silent_deletion never fired. R5 (v2) is the best report of any condition in either eval: 9/12 themes, 0 unsupported, 0 miscalibrated.
- 2026-09-03 — Trap count rose 2.0 → 3.0 but it's denominator: you can only fail the reuse trap if you find reuse. On traps applicable to both versions, v2 is level or slightly better.
- 2026-09-03 — Still failing: Penni's pro-validation dissent (5/6). Profiles pointed the critic at the outlier; it reached for Stephen instead. Honest open item.
- 2026-09-03 — "Cited but not extracted": T-11 and T-15 absent from all six reports while the turns containing them are cited in all six for something else. The model takes one finding per turn. That's v3.
- 2026-09-03 — Decision: v2 ships. Package the tool, then write.
- 2026-09-03 — Total API spend for the whole project, brief to case study: $30.84. Twenty-three runs, two evals, a full-corpus synthesis, every crash and discarded run included.

## Part-1 addendum — the v3 evaluation (2026-09-06)

- 2026-09-06 — Instrument frozen and tagged `v2.1-eval` (commit `3675228`) before any v3 code was written. It is v2-eval as scored in Eval 2 plus exactly one deterministic change, the counter-receipt handling of 2026-09-05 (`ec58797`). That "exactly one" was verified rather than asserted, by reading every commit touching the instrument since the Eval 2 runs of 2026-09-03: `synth/agents.py` one hunk; `synth/prompts.py` gained `STRUCTURE_SYSTEM`/`STRUCTURE_USER`, used only by the critique-document surface and never by the loop; `synth/synth.yaml` differs from the Eval 2 config by a header comment only (`synth-loop` → `Motif`), rules and thresholds byte-identical; `core/loop.py` untouched. The table is in `docs/specs/motif-v3-eval.md`. Worth writing up: the freeze is only worth anything if the claim is checked, and checking it took one `git log -p` over four paths.
- 2026-09-06 — Four items from the part-2 and part-3 builds were added to the v3 change list, each with the run that exhibited it: (a) the critic citing an interviewer turn in its own objection (`runs/20260904-165114-critique-doc`, I-06 → `sam:0068`, whose speaker is "Researcher 1"); (b) a pasted document's recommendations section absorbed into `opportunity` fields instead of checked; (c) the same turn cited twice as two receipts (I-06/`david:0026` in the Figma run; Eval 2's R1 scoring note records the same for I-04 and I-11); (d) whether `confidence_threshold` should scale with corpus size.
- 2026-09-06 — Ruling on (b): report as unevaluated, do not structure recommendations as claims. Structuring a recommendation as a claim would invent an evidence set the document never offered, and the critic would then fail it under `bad_citation` — noise, not signal. The honest output is to say what was not checked, which is what `skipped_rules` already does for `missing_theme`. This is the same principle as "silence is never approval", applied to the critic's own coverage rather than to the synthesis.
- 2026-09-06 — Ruling on (c): warn, not fail. The claim is still supported when a receipt is duplicated; the padding is cosmetic. A fail would send the reviser to rewrite a sound insight, which is how a precision rule turns into a recall loss.
- 2026-09-06 — (d) deferred to v4 with a stated reason, not silently dropped: changing what "high" and "medium" mean re-scores every prior miscalibration count and the whole trap set against a new definition, which is a second scoring session and a second baseline — outside the $25–40 budget. The proposal (`high_min_sources = max(2, min(4, ceil(0.6 n)))`) is written down in the spec so v4 starts from arithmetic rather than from the observation.
- 2026-09-06 — Deviation from the spec's `dedupe` wording, recorded because it changes the cost: the spec asked for a "deterministic-plus-model pass after revise"; built instead as a deterministic critic rule (`duplicate_insight`, Jaccard over title and claim vocabulary, threshold 0.55) that the existing reviser acts on like any other failure. Saves one Sonnet call per iteration, about $0.18 per run and $0.55 across the condition, and reuses the loop that already exists. If the rule fires and the reviser ignores it, that is the finding and a dedicated pass becomes a v4 item.
- 2026-09-06 — Pre-registered bars written before the first paid run, with the baseline arithmetic beside each, read from `eval2/scoring.md` and `eval2/metrics.csv`: T-11 and T-15 present 0/3 each; theme sum over the other ten themes 8.0, 9.0, 8.0 → mean 8.33; unsupported 2, 0, 0 → 0.67; miscalibrated 1, 0, 1 → 0.67; P-03 failed 3/3; `critic_pass` reached 0/3. The honest reading is written into the spec beside the bars: with n=3, 2/3 against a 0/3 baseline is not significant at any conventional level (Fisher exact p ≈ 0.4), and a one-point theme move is inside the rater consistency Eval 2 recorded. The bars are decision rules, not significance tests, and they are worth acting on where the baseline is *never* rather than *sometimes*.
- 2026-09-06 — Cost model built from the Eval 2 runs' own call logs (`runs/20260903-18*/calls/*.json`) rather than from the totals: intake $0.118, synthesis $0.411, critic $1.180 (3 × $0.393), revise $0.570 (2 × $0.285) = $2.279 per run, against measured totals $2.2584 / $2.3120 / $2.2658. Projections: control $2.28, all-v3 $2.70, opus-critic $4.05 (the critic is 52% of a run's cost and Opus 5 is 2.5× Sonnet 5 per token), cap5 $3.64.
- 2026-09-06 — The spec's own run plan does not fit its own budget, and saying so is the finding: four conditions at 3 runs each is $37.99, plus $0.50 for item (b)'s two critique passes, is $38.49 against a $25–40 band — $1.51 of margin, less than one run of any condition. A single crash breaks the band. Plan B (3 runs on control and all-v3, where the bars are decided; 2 runs on opus-critic and cap5, which ask for a stated finding rather than a rate out of three) is $30.80 and leaves ~$9. The choice is the user's, taken before anything is spent.
- 2026-09-06 — Building the tests found a real property of the loop that the eval will now report: with the cap raised to 5, a critic that repeats the same objection still stops the run at iteration 2 under `no_progress`. The first version of the cap-5 test stubbed a revise that changed nothing and got 2 iterations instead of 5; the loop was right and the test was wrong. Kept as its own test (`test_no_progress_still_stops_the_raised_cap`), because it means "cap 5" cannot be read as "5 rounds of critic" — it is a ceiling, not a schedule, and open item 5 has to be answered with that in view.
- 2026-09-06 — v3 eval, condition control (Plan B, 3 runs): `runs/20260906-190902-C-e3-control{1,2,3}`, tag v2.1-eval config `eval3/configs/control.yaml`. All 3 stop=max_iterations, no crashes. control1 15 insights, 3 iters, $2.3578, 1359.8s; control2 13 insights, 3 iters, $2.0425, 1085.2s; control3 15 insights, 3 iters, $2.2980, 1353.3s. Condition total $6.6983. Running total against Plan B's $30.80: $6.70.
- 2026-09-06 — v3 eval, condition all-v3 (Plan B, 3 runs): `runs/20260906-193549-C-e3-allv3{1,2,3}`, config `eval3/configs/all-v3.yaml` (second_finding, duplicate_insight, duplicate_receipt, critic_citation, dissent_at_intake). All 3 stop=max_iterations, no crashes. allv31 21 insights, 3 iters, $2.8470, 1698.9s; allv32 19 insights, 3 iters, $2.3092, 1345.9s; allv33 19 insights, 3 iters, $2.3643, 1388.3s. Condition total $7.5205. Note: insight counts (19–21) noticeably higher than control (13–15), consistent with second_finding adding coverage; whether it's real recall or over-splitting is for blind scoring, not this log. Running total: $6.6983 (control) + $7.5205 (all-v3) = $14.2188 against Plan B's $30.80.
- 2026-09-06 — v3 eval, condition opus-critic (Plan B, 2 runs): `runs/20260906-200614-C-e3-opus{1,2}`, config `eval3/configs/opus-critic.yaml` (Opus 5 as critic, otherwise control). Both stop=max_iterations, no crashes. opus1 18 insights, 3 iters, $3.7102, 1351.1s; opus2 18 insights, 3 iters, $3.9521, 1136.9s. Condition total $7.6623 — close to the spec's projected $4.05/run ($8.10 for 2). Running total: $14.2188 + $7.6623 = $21.8811 against Plan B's $30.80.
- 2026-09-06 — v3 eval, condition cap5 (Plan B, 2 runs, final condition): `runs/20260906-203138-C-e3-cap5{1,2}`, config `eval3/configs/cap5.yaml` (max_iterations 5, otherwise control). Both stop=max_iterations at iteration 5 — neither hit `critic_pass` or `no_progress` in 5 rounds, i.e. the critic kept finding *new* things to object to through all 5 rounds rather than repeating itself or being satisfied. cap5_1 15 insights, 5 iters, $3.2523, 1649.0s; cap5_2 18 insights, 5 iters, $3.3629, 1748.1s. Condition total $6.6152. Running total: $21.8811 + $6.6152 = $28.4963 against Plan B's $30.80 — $2.30 under.
- 2026-09-06 — v3 eval, all 10 runs complete, no crashes, no re-runs needed. Totals: control $6.6983 (3 runs), all-v3 $7.5205 (3), opus-critic $7.6623 (2), cap5 $6.6152 (2). **Grand total $28.4963** against Plan B's $30.80 — $2.30 under. Blind pack built: `python scripts/eval_pack.py <10 run dirs> --out eval3` (seed 7, the script's default) → `eval3/blind/R1.md`...`R10.md`, `eval3/key.json` (sealed, not opened), `eval3/metrics.csv`, `eval3/scoring-template.md`. Caveat carried forward from Eval 2's identical script: `metrics.csv`'s `run_id` column spells out each run's tag (e.g. `20260906-193549-C-e3-allv31`), which names its condition in plain text — same as `eval2/metrics.csv` did for v1/v2. `metrics.csv` must not be opened before or during blind scoring, exactly like `key.json`; only `blind/*.md` and `scoring-template.md` are safe to read now. Spot-checked the blind reports for leaks (tag strings, "contested", "iterations:") — clean.
- 2026-09-06 — Eval 3 scored blind, R1→R10, key opened only at the end. `second_finding` did exactly what it was built for: it fired 8, 8 and 9 times across the three all-v3 runs and nowhere else, and in **iteration 1 of all three** it named `david:0022` and `penni:0031` by turn id with the second finding each carried. T-11 and T-15 went 0/3 in Eval 2 to **3/3 at full credit**, scored blind by a rater who did not know the rule had fired. The tightest causal chain in three evals: rule names the turn, reviser adds the insight, rater scores the theme.
- 2026-09-06 — The "cited but not extracted" failure, isolated: `penni:0031` was cited in **10 of 10** reports and became a claim in only the three all-v3 ones. `david:0022` was cited in 7 of 10 and became a deletion-as-waste claim in exactly the 3 all-v3 runs. R5 is the clean control case — it cites `david:0022` for the navigation barrier and leaves the deletion sentence in the same turn unused. Citing a turn was never the problem; reading it twice was.
- 2026-09-06 — A bar can pass and prove nothing, and saying so is the finding. P-03 (Penni's pro-validation dissent, failed 5/6 in Eval 2) passed 3/3 under all-v3 — and 2/3 under control, and 4/4 under the other two conditions: **9 of 10 reports overall**. `dissent_at_intake` cannot be credited when the condition without it fixed it too. Ruling: don't ship it yet. Same shape for `duplicate_insight`, which never fired in any run at its 0.55 threshold — bar 4 met, rule untested.
- 2026-09-06 — Trap count rose again for the condition that found more (1.7 → 3.0) and again it's denominator: P-02 can only be failed by a report that finds T-11, and all three all-v3 runs found T-11 and then failed to attach Michelle's dissent to it. On the six traps applicable to every report the conditions are flat (control 5/18, all-v3 6/18). Second eval running where the recall win prints as a precision loss unless you check what was in the denominator.
- 2026-09-06 — A recall rule creates a new precision exposure: the insight `second_finding` adds arrives with no counter-evidence, and `missing_counterexample` never revisited it in any of the three runs. P-02 failed 3/3 on exactly the insights the new rule had just produced. v4 item — a newly added finding should be handed to the counter-evidence search on the next iteration.
- 2026-09-06 — The loop never passes. `critic_pass` reached in **0 of 10 runs**, at cap 3 and cap 5; `no_progress` never fired; every run stopped on the ceiling with objections outstanding. Both cap-5 runs went the full five rounds for +0.58 theme points, about $1.10 and 7 minutes of extra critic each. Cap 3 stays, and "the loop can't reach its own stopping condition" is a v4 item, not a footnote.
- 2026-09-06 — P-10 (Bruce's own-data reanalysis, the nuance sitting unused inside `bruce:0043`) was passed for the first time in three evals: R8, opus-critic, quoting the line itself as counter-evidence to its own reuse insight. The trap applied in 13 reports across Evals 2 and 3 and was failed in 12. R8 is also the best report anyone has produced: 11.0/12 themes, zero traps failed.
- 2026-09-06 — Opus as critic clears its own decision rule (+1.58 themes, $3.83/run against a $5.00 ceiling) on **two** runs whose range overlaps control's three. Ruling: documented option for a high-stakes run, not the default, and re-test at n=3 before promoting it. The bar was written to be actionable, not to be gamed by the condition with the smallest n.
- 2026-09-06 — Unsupported citations came out **0 of 40** across ten reports, against 0.7 per report in Eval 2 — but that is *zero in a fixed sample*. The rule (insights at positions 1, 4, 7, 10) was fixed before scoring and held even where insight labels ran out of order, so it can't have been steered; it still only sees four insights out of 13–21. One out-of-sample defect turned up while reading around a sampled insight (R10's I-09 counter characterising David from a turn that describes a hypothetical). Report the sampling rule with the number, or the number is a claim the method can't support.
- 2026-09-06 — No v2 report was re-scored this round, so Eval 3 has no direct rater-consistency measure. What it has instead is seven dated, user-ruled conventions recorded in `eval3/scoring.md` as each was needed and applied to every later report — including one that stopped a double-charge (a missing counter was being deducted from the theme *and* failed at the trap). Convention drift documented beats consistency asserted; it also means control-vs-Eval-2 is not like-for-like, and the comparison that carries weight is within-session between conditions.
- 2026-09-06 — Eval 3 cost $28.50 for 10 runs against a $30.80 Plan B budget, every condition under its projection (control $2.23/run vs $2.28 projected; all-v3 $2.51 vs $2.70; opus-critic $3.83 vs $4.05; cap5 $3.31 vs $3.64). Bar 10 (item (b), unevaluated recommendations sections) was **not run** — the flag is off in all four configs and the $0.50 live pass was never made. Recorded as not run rather than quietly folded into the tests that do cover it.
- 2026-09-06 — Full-project API spend, local plus hosted. Command: `python3 -c "import json, glob; [print(json.load(open(f)).get('cost', 0.0)) for f in sorted(glob.glob('runs/*/meta.json'))]"` (42 meta.json files in this checkout, top-level `cost` field, `RunLogger.finish()`), grouped by run-id pattern into the same eval buckets used above. Eval 1 (`-A/B/C-eval[123]b?`, 13 runs including the v1.1 reruns): $13.3645. Eval 2 (`-v2-eval[123]`, 3 runs): $6.8362. Eval 3 (`-e3-*`, 10 runs): $28.4963, matching the grand total logged 2026-09-06 above exactly. Everything else — first/second/third/fourth-receipts, the two full-corpus runs (`20260903-195307-C` $4.5251, `20260904-154633-C` $4.8822), mcp-refactor, and the nine critique/critique-doc dev runs, 16 runs total, every crash and discard still on disk included (`second` shows $0, a 0-call abandoned run, same shape as the two $0 hosted runs) — subtotals to $17.7239. **Local total: $66.4209** across 42 runs. Hosted total, unchanged from the figure recorded 2026-09-06 in `docs/part3-notes.md` (commit `2ae47a8`): **$5.7838** across 11 records. **Combined project API spend: $72.2047.**
- 2026-09-06 — Release 0.4.0, the v3 default. `synth/synth.yaml` ships `second_finding`, `critic_citation`, and `duplicate_receipt` as on-by-default critic rules; `duplicate_insight` and `features.dissent_at_intake` stay in the file, commented, per the Eval 3 rulings (bar 4 unattributed, bar 5 unattributed — see the entries above). PyPI, the MCP registry (`io.github.sleepycobalt/motif`, fresh `login github` device flow, the token from 0.3.0 having expired overnight — same gotcha as last release), and the hosted engine (`fly deploy`, `/healthz` now echoes its own `version` field for the first time, confirmed live `0.4.0`) all moved together, so every surface a user can reach — pip install, MCP client, and the Figma plugin's free tier against the hosted service — now runs the v3 ruleset by default. Tagged `v3` on the release commit: the third named instrument version to ship as a default, after v1 and v2.
- 2026-09-09 — The forward-reference grep used \| alternation, which macOS git's basic regex silently ignores. It returned empty and the repo was reported clean on that basis; the -E rerun found twelve hits. Silence read as approval, in the tooling of a project whose first rule is that it never should — the fifth instance in two days of a check standing in for the thing it checks.
- 2026-09-09 — Greps in this repo use git grep: a working-directory grep -r walks runs/, which is gitignored because it holds transcript text, and dumped some. The privacy property has to hold in the tools used on the repo, not only in the engine.
