# Part 3 notes — Motif for Figma

Running list of moments worth writing up for the part-3 case study (the hosted engine, the plugin as product, tiers, first users, first revenue). One dated line per moment. Numbers and quotes are copied from the file named in the line, never recalled.

- 2026-09-04 — Part 3 opened. Read `docs/specs/motif-figma-plugin.md` (v2, the commercial surface), `docs/specs/motif-mcp-server.md`, and `CONTRIBUTING.md`. Proposal written to `docs/specs/part3-build-plan.md` before any code; awaiting ruling on hosting, Design-editor timing, and payment rails.
- 2026-09-04 — Engine constraint for BYOK: `core/llm.py` builds one module-level `Anthropic()` from the environment (`client()`), so the hosted service cannot use a per-user key without an engine change. Proposed: a `contextvars.ContextVar` for the client, set per job, defaulting to the environment so the CLI and MCP local mode are untouched.
- 2026-09-04 — Why a job API rather than one request per tool: from `docs/part2-notes.md`, the refactor regression run took 440.9 s and the recorded 15-transcript run took 2,064.6 s ($4.88). Submit / stream / fetch survives proxies and a closed plugin window; the draft contract in `surfaces/mcp/remote.py` (synchronous calls) will be replaced.
- 2026-09-04 — Privacy line in the spec ("processed for the run and discarded; nothing stored") maps onto the logger's existing `redact=True`: digests instead of prompt bodies, no corpus snapshot. The hosted service keeps the processed corpus in memory for the job plus a short TTL, then drops it. Consequence: remote `motif_receipts` only works within the TTL; the plugin gets receipts inline in the insights and never needs the endpoint.
- 2026-09-04 — Editor gap: stickies and connectors are FigJam-only plugin-API nodes; the Design editor has sections but no stickies. The board writer needs a second renderer (frames and text) for Design. Proposed order: FigJam first, Design at the end of stage 3.
- 2026-09-04 — Payments constraint: Figma's payments API sells one-time purchases or subscriptions, not a consumable balance, so prepaid credits need an ETOT-owned ledger topped up by Stripe Checkout via webhook. Matches the spec's Stripe fallback. To verify against Figma's docs when the paid tier is built.
- 2026-09-04 — Discipline miss found: the PyPI publish session (commit `927120c`) appended notes to `docs/part2-notes.md` but no three-line entry to `docs/log.md`. Added retroactively today, marked as such.
- 2026-09-04 — Listing asset gap: the icon source ("the field thumbnail, reduced" in the spec) is not in the repo; `find` for thumb/icon/field returned nothing outside `.venv`.
- 2026-09-04 — Ruling from the user: Fly.io; Design renderer at the end of stage 3; Stripe Checkout with an ETOT ledger, designed in stage 1 and built only when the prerequisites are dated; add a per-IP concurrency cap and a per-job transcript size cap on the BYOK path; the listing icon comes from the Motif field thumbnail (user supplies it); commit and push at the end of every session.
- 2026-09-04 — Stage 1 built offline: `core/llm.py` gained a per-context client (`using_key`) so a hosted job runs on its caller's key; `core/config.py` accepts a context key in place of the env var; `surfaces/hosted/` (app.py job API, jobs.py registry with caps and TTL, credits.py ledger, Dockerfile, fly.toml, README); `surfaces/mcp/remote.py` rewritten as a client of the job API and the MCP server's five remote branches wired to it; `tests/test_hosted.py` (11 tests: service through the test client, remote client on a real port, MCP server as a subprocess in remote mode). Suite: 30 passed in 4.19 s.
- 2026-09-04 — Surprise, and the most important finding of the session: the logger's `redact=True` digested the prompts but kept the model's response text verbatim in `calls/*.json`, and synthesis responses quote the transcripts (the receipts). The privacy claim in the plan ("redacted logs contain no transcript text") was false until a test asserted it: `test_synthesize_job_end_to_end` failed with "transcript text leaked into 003_synthesis.json". Fixed in `core/logger.py`: response text is now a digest too; `data` is dropped from the record; usage, timing, and stop reason stay. Write-up angle: a privacy property is a test, not a sentence in a README.
- 2026-09-04 — Second bug caught by the tests: the remote client merged the engine's result over the job id, so `run_id` in the tool result was the guessable run directory name instead of the random job id that later calls need. Fixed (`_as_tool_result`).
- 2026-09-04 — Environment surprise: `.venv/bin/pip` has a shebang pointing at `/Users/zzz/opt/claude/agentic-loop/synth-loop/.venv/bin/python3`, the venv's original path before the repo was renamed. `.venv/bin/python -m pip` works; the `pip` shim does not. Second time an "installed" environment has turned out to be a claim.
- 2026-09-04 — Corpus size finding for the upload caps: the 17 sample `.docx` files are 28 MB (Jamie.docx 5,879,730 bytes) because each embeds three ~1.7 MB Google Sans fonts (`unzip -l`), while `data/processed/*.txt` totals 620 KB. Caps set at 12 MB per file and 64 MB per job; stage 2 should weigh extracting paragraphs in the plugin before upload rather than sending 37 MB of base64.
- 2026-09-04 — MCP SDK 2.x gotcha (second of the day): `call_tool(progress_callback=...)` must be an async function; a plain lambda raises "object NoneType can't be used in 'await' expression" inside the SDK's dispatcher, and the error only shows in captured logs.
- 2026-09-04 — Deployment state: `flyctl` v0.4.99 installed via Homebrew; not authenticated ("no access token available"); `fly auth login` needs the user's browser. Region set to `ord` (the machine's timezone is America/Chicago). Local smoke: `motif-hosted` on port 8765 answered `/healthz` with `{"ok":true,"paid_tier":false,"jobs":0}`. Not yet done: deploy, and the stage-1 QA gate (MCP server in remote mode synthesising the sample corpus against the deployed service).
- 2026-09-04 — Follow-up flagged: PyPI's `etot-motif` 0.2.0 carries the old synchronous remote contract, so remote mode reaches installed users only with a 0.3.0 release after the deploy is verified.
- 2026-09-05 — Deploy gotchas, in order: `fly auth login` refuses to run under Claude Code's `!` prefix ("requires an interactive terminal"), so the user logged in from a separate terminal and the CLI's saved token was picked up; `fly apps create` refused until a card was on the account ("We need your payment information to continue"); `[build] dockerfile` in fly.toml is resolved relative to the toml's directory, not the repo root (first deploy failed looking for `surfaces/hosted/surfaces/hosted/Dockerfile`); the build context was 250 MB across 9,920 files (.venv 119 MB, runs 60 MB, .git 34 MB, data 32 MB, per the deploy warning) until a root `.dockerignore` allow-listed only pyproject, README, LICENSE, core, synth, surfaces. Final image 62 MB.
- 2026-09-05 — Live: app `motif-hosted`, region ord, machine 801e44c60de958, volume `motif_data` 1 GB, https://motif-hosted.fly.dev; `/healthz` returns `{"ok":true,"paid_tier":false,"jobs":0}`. Sizing: one shared-CPU machine, 1 GB memory, kept running. First hosting cost line for the case study once the first invoice arrives.
- 2026-09-05 — Stage 1 QA gate PASSED. The MCP server in remote mode (`MOTIF_REMOTE_URL=https://motif-hosted.fly.dev`, spawned by a client script) synthesised michelle + david through the hosted service. From `docs/exhibits/stage1-remote-qa/synthesize-result.json`: job `5GfWRXE3yUt43vfGyPhNSmzb`, hosted run `20260905-054758-C-hosted`, 11,482 words, 15 insights, contested I-02, I-05, I-08, 3 iterations, stop=max_iterations, $1.1409, engine wall 719.5 s, client wall 721.4 s, 21 progress notifications received by the host. From `run-record.json`: redacted=true, 8 calls, 224,535→73,279 tokens; final verdict failures I-08 missing_counterexample, I-02 unsupported, I-05 unsupported. `motif_receipts` by job id returned david:0022 found and nobody:0001 missing; `motif_board` returned 15 sections and 15 scripts; `motif_runs_get` returned the redacted record. Report copied to `docs/exhibits/stage1-remote-qa/report.md`.
- 2026-09-05 — Hosting overhead is 1.9 s on a 12-minute run (client wall 721.4 s against engine wall 719.5 s): the event stream kept pace and no proxy cut it. The run cost twice the local regression from `docs/part2-notes.md` ($0.5529, 1 iteration, 440.9 s) because this loop ran all 3 iterations, not because of hosting. From `progress.log`: synthesis 229.96 s; critic 86.32 s, 89.32 s, 107.38 s.
- 2026-09-05 — Privacy check on the volume itself (`fly ssh console`): `/data/runs/20260905-054758-C-hosted` is 556 KB with `calls`, `iterations`, `meta.json`, `notes.txt`, `output.json`, `output.md`; no `corpus/` directory anywhere under `/data/runs`; `calls/003_synthesis.json` holds `system`, `user`, and `text` as `{chars, sha256}` (user prompt 90,569 chars, response 16,447 chars). A five-word phrase from a cited turn appears only in `output.md` (the run's own report, receipts included) and in the scratch dir `/tmp/motif/motif-job-cxgr9cup/processed/`, both of which the expiry sweep deletes an hour after the job finishes (covered by `test_expiry_drops_corpus_and_run_content`; not re-observed live within this session).
- 2026-09-05 — Same notification finding as part 2: the QA client received 0 log notifications because it set no logging level, and 21 progress notifications because it passed a callback. The MCP SDK's deprecation warning on the logging capability (SEP-2577) prints on every emit in the server's stderr; harmless, noisy.
- 2026-09-05 — Found while wiring the QA client: in remote mode the MCP server never called `load_dotenv`, because only the config loader did and remote mode skips it, so a user's `.env` key would not have been forwarded. Fixed in `server.main()`; the QA run relied on the fix (no key was passed in the subprocess environment).
- 2026-09-05 — Next: 0.3.0 to PyPI so installed users get the working remote mode (0.2.0 has the old contract); then stage 2, the plugin on the free tier.
- 2026-09-05 — Release 0.3.0: version bumped in `pyproject.toml`, `surfaces/mcp/server.py` (MCPServer version), and `surfaces/mcp/server.json` (server and package); hosted app already reported 0.3.0. Built, `twine check` passed on both artifacts, uploaded with the token as `TWINE_PASSWORD` on the one command; https://pypi.org/project/etot-motif/0.3.0/. Pre-upload checks on the local wheel in an empty venv: `motif --help`, local stdio handshake reporting `motif 0.3.0` and five tools, and two zero-cost remote-mode probes against the live service (an empty corpus fails the hosted job at ingest and the tool relays "all 1 transcript file(s) were empty"; receipts on an unknown job relays the service's 404).
- 2026-09-05 — Registry: yesterday's `mcp-publisher` token had expired (a fresh `login github` device flow was needed; the token is short-lived, so every publish needs the click). `publish` from `surfaces/mcp/` reported "Server io.github.sleepycobalt/motif version 0.3.0"; the public API now lists both 0.2.0 (active, not latest) and 0.3.0 (active, latest), both pointing at their PyPI versions.
- 2026-09-05 — PyPI index race: `pip install etot-motif==0.3.0` run immediately after the upload failed with "from versions: 0.2.0" even though the registry had already validated 0.3.0 against PyPI's JSON API; the same command succeeded on the first retry about two minutes later. The simple index behind pip lags the JSON API by a minute or two.
- 2026-09-05 — Verified from PyPI in an empty venv (`etot-motif[mcp]==0.3.0`): `motif --help`; local handshake `motif 0.3.0`, five tools, verbatim error on a missing run; the two zero-cost remote probes pass. A live remote synthesis from the installed package (michelle + david, max_iterations=1) was then launched; result recorded in the next line.
- 2026-09-05 — Live remote synthesis from the PyPI-installed 0.3.0 PASSED (`docs/exhibits/stage1-remote-qa/pypi-0.3.0-live.json`): job `tvlHta4iJRZXnxHoxdYUzVPu`, hosted run `20260905-164234-C-hosted`, michelle + david, max_iterations=1, 13 insights, contested I-04 and I-11, $0.458, engine wall 338.3 s, client wall 339.2 s, 13 progress notifications; receipts by job id found david:0022; board 13 sections; run record redacted=true with 4 calls. Same shape as the part-2 local regression (`docs/part2-notes.md`: 13 insights, 4 calls, 440.9 s, $0.5529), so the hosted path costs nothing extra beyond 0.9 s of wall time on this run.
- 2026-09-05 — Stage 2 opened: the plugin on the free tier, no board. Ruling from the user: extract docx paragraphs in the plugin before upload; QA gate as planned (install to result in a fresh FigJam file under ten minutes; every screen checked at desktop and phone widths for clipped or overflowing elements before reporting). Environment: no Node on the machine (`brew install node`); `.venv/bin/pip` still broken (use `python -m pip`).
- 2026-09-05 — Service change for the plugin: the plugin UI runs in a sandboxed iframe whose origin is `null`, so every call is a CORS preflight. Added `CORSMiddleware` (any origin; GET/POST; headers Content-Type, X-Motif-Key, Authorization, Last-Event-ID) with a test, redeployed (image 70 MB). The key travels in a header, never a cookie, so an open origin policy exposes nothing the caller did not already hold. Verified live: `OPTIONS /v1/jobs` from `Origin: null` returns `access-control-allow-origin: *`.
- 2026-09-05 — Plugin built in `surfaces/figma/`: TypeScript, esbuild, no framework; `code.ts` (main thread: client storage for the key and the last job id, notifications, resize), `ui.ts` (screens, files, the run), `api.ts` (submit, follow SSE with resume, fetch), `docx.ts` (zip walk + `DecompressionStream("deflate-raw")` + `word/document.xml` paragraphs, no library), single-file `dist/ui.html` 33 KB, `dist/code.js` 2 KB. Manifest allow-lists only `https://motif-hosted.fly.dev`. The board writer is a disabled button labelled "next release".
- 2026-09-05 — docx parity, `surfaces/figma/test/docx_parity.mjs`: the plugin's extraction, run under Node with `@xmldom/xmldom`, on all 16 sample `.docx` (29.8 MB) produced 587 KB of text; ingesting that text and the original docx through `synth.ingest` gave equal manifests (15 transcripts, 101,042 words; one file skipped as empty on both paths) and, turn by turn, 1,868 turns compared, 1,867 byte-identical, 1 differing only by a line break inside a paragraph (python-docx keeps `\n` inside the turn; the plugin makes it a space, because the upload is one turn per line), 0 mismatches. The first run found this: Lisa (2) and Peter (1) had `<w:br/>` inside paragraphs, and a newline there would have split a turn in two on the text path.
- 2026-09-05 — Harness for the QA gate: `dist/ui.html` runs in a plain browser tab with `localStorage` standing in for Figma's client storage, `?files=` and `?demo=` hooks load transcripts and a saved result, and `test/harness.html` frames it at 440 (plugin default), 375 (phone), and 320 px. Chrome's window would not shrink below 1440 px on this machine (`resize_window` reported success, the viewport stayed 1440), so real iframes at those widths were the honest way to test; the UI treats a `?harness` frame as outside Figma.
- 2026-09-05 — Bug caught by the harness: the "A run is still going from last time" card showed with no run, because `.card { display: flex }` beat the browser's `[hidden]` rule (an author class rule outranks the UA stylesheet). Fix: `[hidden] { display: none !important; }`. It would have shown on every first open in Figma.
- 2026-09-05 — Every screen scanned at 440, 375, and 320 px (element rectangles against the viewport, `scrollWidth` against `clientWidth`, page horizontal scroll): key, setup with two docx transcripts extracted in the browser (Michelle 129 paragraphs / 5,644 words, David 76 / 6,155; server ingest then reported 128 and 74 turns, 11,482 words, identical to the docx path), running, result from the recorded 15-insight run (collapsed and with insights expanded), error with the rejected-key hint. Zero issues at any width; screenshots in `docs/exhibits/stage2-plugin/`.
- 2026-09-05 — The error screen relays the engine's message verbatim (Anthropic's 401 `API key is invalid.` for the placeholder key) and, above it, a plain hint for the three cases a user can act on: rejected key, no credit, engine unreachable. `last_job` is cleared on failure, so a failed run never offers itself for resumption.
- 2026-09-05 — Browser-path live run PASSED (`docs/exhibits/stage2-plugin/browser-run.json`, report in `browser-run-report.md`): launched from the plugin UI in the 440 px harness frame with the real key read into the page from the local `.env` (never through the chat), docx extracted client-side, job `ngqJoYXl17yhFKHVFdosoV84`, hosted run `20260905-171330-C-hosted`, 11,482 words, 12 insights, contested I-05 and I-11, 3 rounds, stop=max_iterations, $1.0328, engine wall 660.1 s, 20 progress events shown live in the running screen; the result screen rendered 12 insights with 2 contested badges and the tiles read "12 2 3 1.03 11.0"; `last_job` cleared on completion. Zero layout issues at 440 px on the running and result screens. This is the same corpus and settings as the part-2 recorded runs, so the plugin path costs what the CLI path costs.
- 2026-09-05 — Clipboard finding: in the harness both `navigator.clipboard.writeText` and `execCommand("copy")` failed on a synthetic click ("Document is not focused"), which is expected without user activation but would also be the outcome in any sandboxed iframe that denies clipboard access. Instead of a failed toast, the copy button now falls back to revealing the report in a selected, read-only box with a "press Cmd+C" note. Verified at 440 and 375 px: the box shows the full 58,847-character report with no overflow. Whether Figma's iframe grants clipboard-write on a real click is a question for the Figma run.
- 2026-09-05 — Tooling note: Chrome's `Page.captureScreenshot` timed out three times right after the report textarea was selected in two frames; navigation and JavaScript kept working. Screenshots for the copy-fallback state therefore come from the scan output, not an image.
- 2026-09-05 — Not done in this session, by construction: the ten-minute Figma run. Development plugins load only in the Figma desktop app, so the install-to-result timing and the clipboard check inside Figma's iframe are the user's run, from `surfaces/figma/README.md` ("Run it in Figma"). The plugin is not presented as done until that run is logged here.
- 2026-09-05 — Stage 2 QA gate, the user's Figma run (numbers from the user's report in chat): install to run-start 8 m 20 s including looking up the key and reading the instructions, "call it 5–6 minutes for a repeat user"; the run 12 m 33 s against the plugin's own 12.4 minutes; install to result about 21 minutes. Copy report: copied to the clipboard inside Figma, full Markdown with receipts, no fallback box. Nothing clipped or broken on any screen. Ruling: the ten-minute target applies to install-to-run-start, not install-to-result, since a run alone takes twelve minutes; the gate is redefined that way in the plan and the plugin README. Stage 2 accepted; proceed to stage 3.
- 2026-09-05 — Two observations from the user for stage 3, not blockers. (1) With two transcripts every insight shows "low" because high needs four participants (`confidence_threshold`, `config/synth.yaml`); the chip should say why, e.g. "low · 1 of 2 participants", or the threshold should scale with corpus size. Chip wording is a plugin change and goes into stage 3; scaling the threshold is an instrument change and stays a proposal until the eval re-run. (2) The UI uses Figma's default blue and Inter; the ETOT design-system session will hand over tokens, so the plugin is to be restyled on the chassis (ETOT blue accent, mono for ids and receipts) once the first components land.
- 2026-09-05 — From the user's read of the Figma run's result: I-11's contested flag is a correct catch of selective quotation (michelle:0077 says "not necessarily physical conversation"). I-06 cites david:0026 twice as two receipts.
- 2026-09-05 — v3 eval item: duplicate receipts. An insight can cite the same turn twice as two evidence entries (I-06, david:0026, in the user's Figma run of 2026-09-05), which inflates the receipt count without adding evidence. Candidate fix: a deterministic check in `synth/agents.py::deterministic_checks` (rule id `duplicate_receipt`, warn) or dedupe at normalise time; either is an instrument change, so it waits for the v3 eval.
- 2026-09-05 — Stage 3 built. Python: `synth/board.py` gained `verdict_layout` (one section per checked claim, coloured by outcome, one sticky per objection, a final section for corpus-level objections) and a `card` block on both layouts (run id, question, corpus, counts, stop reason, cost, time); `engine.board` serves critique runs; the hosted `/board` endpoint accepts any finished job; 33 tests pass. Plugin: `src/board.ts` renders a layout on the current page (FigJam: sections, stickies in two rows, "contested by" connectors, a run card above the grid; Figma Design: the same sections with auto-layout frames and text, no connectors), picks a free origin to the right of existing content, and throws rather than returning a partial board; `code.ts` draws on "build-board" and stores the last result with its layout in client storage; `ui.ts` gained the Build board action, "Open last result", the Check-a-synthesis mode (paste a document, run the critic alone, verdict claim by claim with a Markdown export), the confidence chip that says why ("low · 1 of 2", from the user's observation), and the critic's notes folded into a collapsible block.
- 2026-09-05 — Renderer tests. Fake Figma API (`test/board_render.mjs`): synthesis layout of the stage-1 run, 15 sections, 70 stickies (69 + run card), 7 connectors on FigJam and 0 on Design; verdict layout of `runs/20260904-165114-critique-doc`, 9 sections, 33 stickies; a duplicated sticky key and an empty layout both throw. Two fake-API findings fixed before the real board: connectors' own coordinates dragged the board bounds to the origin (connectors now excluded from bounds), and the harness's board acknowledgement counted the card as a section.
- 2026-09-05 — Real-board check of the plugin's own renderer, bundled by `test/board_script.mjs` and run through Figma's MCP `use_figma` on a throwaway FigJam board (https://www.figma.com/board/ucUAWcb1M6TFdMC8SfgAL1) with four sections of the stage-1 run (I-01, I-02, I-03, I-05): 4 sections, 20 stickies, 1 connector, run card at y = -568; `get_figjam` read back every sticky colour as a palette value (orange, yellow, white, pink, blue, violet; 0 CUSTOM), the connector `1:48` with its "contested by" label and arrow cap, section heights 656 / 792 / 680 / 696 px inside the 1200 px slot, the card 448 px. Exhibits: `docs/exhibits/stage3-board/figjam-section-I-02.png`, `figjam-run-card.png`, `figjam-readback-summary.json`. Same script on a throwaway Design file (https://www.figma.com/design/bANfg7jqtmagAd33xhU2vR): 4 sections, 20 frames, card; frames hug their text (first section 1600 × 421 px; claim 168 px, receipts 134 and 100 px, opportunity 117 px). Exhibit: `design-section-I-02.png`.
- 2026-09-05 — Surprise on the real board: the run card read "11482 words". `Number.toLocaleString` in Figma's plugin sandbox returns bare digits, so the card now formats thousands by hand (`thousands()` in `board.ts`). The verdict layout was exercised on the fake API only, not on a real board; the user's Check-a-synthesis run covers it.
- 2026-09-05 — Harness at 440 / 375 / 320 px, zero layout issues on every new screen: result with Build board and the new chips ("low · 1 of 2", "medium · 2 of 2"), the verdict result (tiles "FAIL 9 21 2", nine claims open with their objections), and the critique-mode setup. Exhibits in `docs/exhibits/stage3-board/`.
- 2026-09-05 — Not done here, by construction: the stage-3 QA gate in Figma is the user's run. Two ways to reach a board: a fresh synthesis (about 12 minutes, about $1 on two transcripts) then Build board; or Check a synthesis on the pasted stage-2 report (one critic pass, about $0.35) then Build board for the verdict. The gate compares the drawn board against `docs/exhibits/recorded-run/board-overview.png` for shape, every sticky present, palette colours only. Stage 3 is not presented as done until that run is logged.
- 2026-09-05 — Stage 3 QA, the user's Check-a-synthesis run on the stage-2 report (from the user's report in chat): 16 claims, 6 fails, 0 warnings; every fail was `quote_mismatch` on a counter-evidence turn with an empty quote (e.g. david:0012: ""). Root cause, predicted in `docs/part2-notes.md` on 2026-09-04 and left unchanged then: `synth/report.py` printed receipts for evidence only, so `parse_motif_markdown` produced counters with empty quotes and the deterministic check counted absence as mismatch. Ruling: Motif's own report must round-trip through the critic with zero mechanical failures. Fix, two parts: the report now prints counter receipts too, and `quote_mismatch` treats a counter-evidence entry with no quote as "no receipt in the source" (skipped) rather than a mismatch; an evidence entry with no quote still fails. Two tests: a report from the fixture insights round-trips with zero mechanical failures; a pre-fix-shaped counter does not fire. 35 tests pass. This is a change to a deterministic rule's handling of one input shape, recorded here as such; the model-judged rules are untouched.
- 2026-09-05 — Same run, Build board: the board drew in full (run card, 16 sections, fail stickies), then the step after drawing threw "in getNodeById: Cannot call with documentAccess: dynamic-page. Use figma.getNodeByIdAsync instead." The manifest's `documentAccess: dynamic-page` forbids the synchronous lookup the zoom step used; `use_figma` had not caught it because the MCP runtime allows both (`typeof figma.getNodeById === "function"` there). Fixed: `code.ts` resolves ids with `getNodeByIdAsync`; `dist/code.js` contains no synchronous call. Verified on the throwaway board that the async lookup finds all six ids (five sections, one connector) and `scrollAndZoomIntoView` runs.
- 2026-09-05 — Same run: opening the plugin in a fresh file showed the empty form, not the stored last result. Fixed: with a key saved, a stored result, and no run in flight, the plugin opens on the result screen with Build board ready; "New run" leads to the form. Colours on the drawn board were FigJam palette; nothing clipped in the panel (user's report).
- 2026-09-05 — Re-verified live after the deploy (`docs/exhibits/stage3-board/critique-roundtrip-after-fix.json`): the pre-fix-format stage-2 report (`docs/exhibits/stage2-plugin/browser-run-report.md`, 12 insights, no counter receipts) through the hosted engine's critique, job `lmIPSVZ36Q0pAf8xM4Sd5lcS`, 241.6 s: parsed deterministically (`motif_markdown`), 12 claims, 4 counters with empty quotes, 0 mechanical failures; the verdict's only failures are model-judged, I-11 `unsupported` (the selective-quotation catch the user confirmed as correct) and I-01 `merged_insights` (warn). The user's run before the fix had 6 fails, all `quote_mismatch`. Reports written from now on carry counter receipts, so the round trip is complete rather than merely tolerant.
- 2026-09-05 — Stage 3 QA gate PASSED on the user's FigJam run; twelve screenshots copied to `docs/exhibits/stage3-plugin/` (01–12). Pre-fix critique board (01): FAIL, 16 claims, 6 fails, 0 warnings, "Not checked: missing_theme"; board 16 sections, 23 stickies = 16 claims + 6 objections + 1 card; fails I-01, I-03, I-08, I-10, I-13, I-16, every one the empty-counter-quote `quote_mismatch` fixed later the same day.
- 2026-09-05 — Synthesis from the plugin, txt path (02–04): David.txt 76 paragraphs / 6,155 words, Michelle.txt 129 / 5,644, "11,799 words, about 64 KB as text"; server ingest 74 + 128 turns, 6037 + 5445 = 11,482 words; job `5aTkyMWhftyvfOQ1iVLhfjfE`, run `20260905-221749-C-hosted`; result 16 insights, 2 contested (I-01, I-10), 3 rounds, $1.14, 12.1 min, stop=max_iterations; chips medium · 2 of 2 on I-01 and I-05, low · 1 of 2 on the rest visible.
- 2026-09-05 — Build board on the first file (05–08): "16 sections, 71 stickies, 4 connectors"; 71 = 16 claims + 32 receipts + 4 counters + 16 opportunities + 2 contested + 1 card; 4 connectors = the 4 counters (I-01, I-04, I-05, I-10); placed to the right of the existing verdict board, so the free-origin rule held on a non-empty page. Run card reads "11,482 words" with the comma — `thousands()` confirmed on a real board. Colours: yellow medium, orange low, white receipts, pink counter, blue opportunity, violet contested and card; no red. Palette-vs-CUSTOM not readable from a screenshot; the 0-CUSTOM read-back stands from `docs/exhibits/stage3-board/figjam-readback-summary.json`.
- 2026-09-05 — Stored result on open (09–12): a fresh FigJam file opened on the result screen with Build board ready; the board drew at the origin with the same 16 / 71 / 4 (toast in 10); I-03–I-06 zoom matches the first board sticky for sticky.
- 2026-09-05 — Clipping: nothing clipped or overflowing in the panel on home, running, result, verdict, or board-built screens at Figma's default width; Figma's own notify toast overlays the panel briefly (10). On the canvas, section titles truncate at the section edge because the title is the whole claim (07, 12) — FigJam rendering, not a defect; open item: ellipsis at a fixed length in `board.ts`, deferred.
- 2026-09-05 — At-a-glance read against `docs/exhibits/recorded-run/board-overview.png`: same shape (card above a two-column grid, claim coloured by confidence, white receipts, blue opportunity, pink counter under "contested by" with a connector, violet for unresolved objections); contested insights are visible zoomed out from colour alone.
- 2026-09-05 — Side-by-side as seen by Claude (`docs/exhibits/recorded-run/board-overview.png` against `docs/exhibits/stage3-plugin/06-board-overview-verdict-and-synthesis.png`): both show two-column grids of light-blue sections with the claim sticky top-left, white receipts beside it, a blue opportunity sticky below, pink counters and violet contested stickies where they occur, and the same section spacing. Two differences, both by design: the stage-3 board has a "Motif · run" card section above each grid, which the part-2 board (drawn through the MCP `motif_board` scripts) did not have; and the stage-3 file also carries the verdict board to the left, whose sections are named `I-nn · fail` or `I-nn · pass` with a pink or green claim sticky, a shape that did not exist in part 2. Section titles in both truncate at the section edge.
- 2026-09-05 — Figma Design, user's run (13–15): fresh file opened on the result screen, Build board ready, panel in Figma's dark theme; "Board built: 16 sections, 71 stickies (no connectors in Figma Design)"; 16 white sections in two columns on the 1E1E1E page with the run card section above; same roles and colours as FigJam, frames hug their text, I-01 and I-10 show pink + violet; Design truncates section titles with an ellipsis where FigJam hard-cuts; Figma's grey frame labels (claim / receipt / opportunity / counter / contested) show above each element and read as a legend. Nothing clipped.
- 2026-09-05 — Record correction: `docs/specs/motif-mcp-server.md` on disk stayed at the v1 text until 2026-09-05, while the part-2 build followed the v2 spec (MCP first, five tools, board via the host) as confirmed in chat on 2026-09-04. The v2 text was written to disk on 2026-09-05. Anyone reading the repo history for part 2 should read `docs/part2-notes.md` for what was actually built.
- 2026-09-05 — Stage 4 opened: the Community listing, free tier only. Draft copy in `surfaces/figma/listing.md`: name Motif by ETOT; tagline "Research synthesis that shows what survived."; three ways to use it (synthesise, check a synthesis, build the board); a receipts-and-contested paragraph; FAQ on keys, transcripts, cost (the plugin's own words: about $1 for two transcripts, about $5 for fifteen), time, format, rules, paid tier; category Research / Whiteboarding; editor types FigJam and Figma Design. Not submitted.
- 2026-09-05 — Link decision: etot.design's Motif page is at `/tools/motif/` with the case study at `/tools/motif/case-study/` (read from the live site on 2026-09-05), not `/motif`; the listing links those, plus `ericfrye.info/ui/ux/motif` and the repo. The site's one-line description of Motif reads "Turns a folder of interview transcripts into findings, then tries to knock them down — and shows you what survived." and the listing's description opens on that register.
- 2026-09-05 — Screenshot picks: board overview `stage3-plugin/11` (synthesis board alone on a fresh file; `06` if the verdict board should appear beside it), contested section `08`, plugin UI `04`, cover the recorded run's I-09 section `docs/exhibits/recorded-run/board-I-09-contested.png` per the spec; optional Design shot `14`. Needed from the user: a post-fix Check-a-synthesis screenshot for the fourth slot; `01` is the pre-fix run and is excluded. Icon: the field thumbnail, pending from the user; no substitute generated.
- 2026-09-05 — Manifest check for the listing: `name`, `editorType` (figjam, figma), `documentAccess` (dynamic-page), and `networkAccess` (allow-list `https://motif-hosted.fly.dev` with a reasoning string) are in place. `id` is a placeholder (`1546218811003944207`); Figma assigns the real id when the plugin is first published from the desktop app, and the manifest must be updated to it. Likely asked for at review and not yet in hand: a support contact (etot.design shows `etot.design@gmail.com`; the spec also wants a stated response time in the listing), an icon (128 × 128) and cover image (1920 × 960), and a privacy policy URL — etot.design has no `/privacy` or `/terms` page as of 2026-09-05 (read from the live site). The listing FAQ states the privacy behaviour in plain words and cites the notes; a policy page on etot.design is the remaining gap.
- 2026-09-05 — Paid tier in the listing: proposal is to hide the `Credits · not yet` control entirely in the published build (a disabled control in a public listing draws questions and reviewers may treat it as an unfinished feature), keeping the code path behind the existing server flag; the alternative is to keep it visible labelled "Credits · coming". Awaiting the user's ruling before any change. No ETOT-key runs of any kind in either case.

- 2026-09-05 — Post-fix Check-a-synthesis on the live plugin path (FigJam, 6:57–7:06 PM CDT): the Markdown report of run `20260905-221749-C-hosted` copied from the plugin, pasted, David.txt and Michelle.txt re-added; job `PDt5IyygwZLrUc1Vni5o2L7V`; "structured 16 insight(s) from Motif-format markdown (deterministic)"; "deterministic checks: 0 failure(s)" — the round trip is clean on the live path. Verdict FAIL, 16 claims, 2 fails, 0 warnings, "Not checked: missing_theme (no intake notes for a pasted document)". Fails: I-01 `missing_counterexample` citing david:0066, david:0067 (matches the original run's contested flag on I-01); I-13 `unsupported` on michelle:0107, michelle:0028 (new). I-10, contested in the original run, passed here: critic variance, recorded not tuned. Board built: 16 sections, 19 stickies = 16 claims + 2 objections + 1 card, placed right of the synthesis board. Cost of the pass, read from the hosted volume at `/data/runs/20260906-000123-critique-doc/meta.json` (UTC-named; `fly ssh console -a motif-hosted`): cost 0.1793 USD, wall 116.6 s, 37,509 in / 10,428 out tokens, 1 call, stop `critique_fail`, finished 2026-09-06T00:03:19Z.
- 2026-09-05 — Screenshots of that run copied from the Desktop to `docs/exhibits/stage3-plugin/` as 16 (form, report pasted, no transcripts), 17 (form, button disabled), 18 (checking, zero deterministic), 19 (verdict, I-01), 20 (verdict, I-13), 21 (verdict, board built). `21` is the fourth listing screenshot; `01` stays excluded as the pre-fix run.
- 2026-09-05 — Polish (a), plugin UI: "Check the synthesis" (and "Synthesise") was disabled with no reason shown. A line now sits under the run button, `#run-why`, saying what is missing: "Needs the transcripts the synthesis rests on." / "Needs the synthesis to check, pasted above (a few sentences at least)." / "Needs at least one transcript." / "…and the research question." / unreadable files: "transcripts that can be read (see the note beside each file)". Hidden when the button is live. Harness at 440 / 375 / 320 px on the critique form without transcripts, the critique form without text, and the synthesis form without transcripts: zero layout issues; composites in `docs/exhibits/stage4-polish/`.
- 2026-09-05 — Polish (b), plugin UI + engine: the verdict screen now carries "API cost" and "minutes" tiles like the synthesis screen (six tiles; the 320 px grid wraps them 3 + 3). The engine's `_critique` return gained `cost_usd` and `wall_seconds` from the run meta (`synth/engine.py`); `VerdictResult` in `api.ts` gained the two optional fields; the verdict board card now prints cost and time as the synthesis card does (`board.ts cardText`). The fixture `test/fixtures/verdict-result.json` carries the real values from its run's `meta.json` (0.4905 USD, 101.5 s). Harness at three widths: zero issues. NOT DEPLOYED: `fly deploy -c surfaces/hosted/fly.toml` was blocked in this session; until it runs, the live verdict screen shows "–" in both tiles. 34 Python tests and the renderer test pass.
- 2026-09-05 — Bug found by the harness while checking (b): the API-cost tile rendered "0.49", no dollar sign. `build.mjs` inlined the script with a string `String.replace`, and a `$$` in a replacement string collapses to one `$`, so the template literal `$${cost}` in `ui.ts` reached `dist/ui.html` as `${cost}`. This has affected the synthesis screen's cost tile in every build to date (the source was always right; earlier plugin screenshots show the tile without the sign). Fixed with function replacers. Verified in `dist/ui.html` and on the rendered tiles: "$0.49".
- 2026-09-05 — Harness scan is now a script: `surfaces/figma/test/harness_scan.py <out_dir>` (Playwright, Chromium) serves the repo, drives each screen under test at 440 / 375 / 320, checks page scroll, `scrollWidth` against `clientWidth`, and element boxes against the right edge, records the reason line and the tile texts, and writes one composite JPEG per screen plus `harness-scan.json`. The renderer test's esbuild artifact `test/.board.mjs` is now gitignored.
- 2026-09-05 — Ruling (1): the `Credits · not yet` control is hidden entirely in the published build (`hidden` on the tier label in `ui.html`); the code path stays behind the server's `MOTIF_PAID_ENABLED` flag; `listing.md` version notes say a paid tier with ETOT-provided credits follows in a later release. The key screen's sentence "Credits without a key are not available yet." is unchanged; say if it should go too.
- 2026-09-05 — Ruling (2): icon per ETOT's per-tool identity rule — each tool's icon is its own generated field, one colour, no letterform; the monogram is reserved for the studio. The user exports the Motif field to `surfaces/figma/listing/icon.png` (128 × 128); recorded in `listing.md` § Assets; source path to be recorded when supplied.
- 2026-09-05 — Ruling (3): support contact hello@etot.design (forwarding confirmed), stated response time "within two working days"; in `listing.md` § Links and in `surfaces/figma/README.md` § Support. Replaces etot.design@gmail.com in the listing.
- 2026-09-05 — Ruling (4): the privacy policy page is an etot-site session; `listing.md` carries a `[privacy URL]` placeholder under Links and lists the page as the last blocker in a new § Blockers before submission (after the real plugin id and the icon/cover).
- 2026-09-05 — Ruling (5): LLC filing has started (single-member); Stripe and the credits ledger stay unbuilt until first users. No change in the repo.
- 2026-09-05 — Second session on the same rulings, picking up where commit `508975c` stopped. Nothing in that commit was redone: the six post-fix screenshots (16–21), the run-button reason line, the verdict cost and time tiles, the hidden Credits control, the support contact in the listing and README, and the notes for items (a), (b) and rulings (1)–(5) were already in place and pushed. The cost the earlier line cites for the post-fix critique pass was re-read from the volume: `/data/runs/20260906-000123-critique-doc/meta.json` (via `fly ssh console`) says cost 0.1793, wall_seconds 116.6, 1 call, 37,509 → 10,428 tokens, stop=critique_fail.
- 2026-09-05 — The blocked step from that session, `fly deploy`, ran here: image 62 MB, `/healthz` ok. Verified on the live path with one critique pass through the hosted API (`docs/exhibits/stage4-polish/live-verdict-cost-time.json`): job `6R0hYQM-Rccnlk6Yj0STOHCq`, run `20260906-010935-critique-doc`, the stage-1 report (15 insights) against david + michelle, parsed as `motif_markdown`, 15 claims, 3 fails, 0 warnings, and the job result now carries `cost_usd` 0.1546 and `wall_seconds` 88.4, so the verdict screen's tiles read real numbers instead of "–".
- 2026-09-05 — Harness re-run with the scan script (`surfaces/figma/test/harness_scan.py`, Playwright installed into the venv for it, Chromium 1 download): all four affected screens at 440 / 375 / 320 px, 0 layout issues; reason lines read "Needs at least one transcript." and "Needs at least one transcript and the research question."; the verdict fixture's tiles read `$0.49 API cost` and `1.7 minutes`, so the `$` survives the build. Output copied to `docs/exhibits/stage4-polish/harness-scan-rerun.json`.
- 2026-09-05 — Icon delivered: `surfaces/figma/listing/icon.png`, 128 × 128 PNG (2,165 bytes; dimensions read from the file header). Source: generated by `etot-site/scripts/icons.py` at commit 23c596d from `etot-site/assets/icons/motif-128.png` (corrected from `assets/icons/<name>`, which was a placeholder read literally); the icon system is `etot-internal/brand/icon-system-spec.md` (a272fd2 and later). The icon placeholder is dropped from the listing's blockers; the cover, the privacy page, and the plugin id at first publish remain.
- 2026-09-05 — Retention sweep for redacted run records, so the privacy page can say "deleted 30 days after the run" before it goes up. `JobStore.sweep_records()` in `surfaces/hosted/jobs.py` deletes any run directory under the runs volume whose `meta.json` `started` is older than `retention_days` (default 30, `MOTIF_RETENTION_DAYS`), directory mtime as the fallback when meta is unreadable; it runs at store construction (startup) and at most once an hour from the existing job sweep, and `/healthz` now reports `retention_days` and `records_swept_at`. Test `test_retention_sweep_removes_old_records_and_keeps_young`: a 31-day-old record with calls/, iterations/, notes.txt and meta.json is removed, a 1-day-old and a 29.9-day-old are kept, a meta-less directory 40 days old by mtime is removed; 36 tests pass. Verified on the live volume: a synthetic record `20260701-000000-C-hosted` (started 2026-07-01, 67 days old) planted next to the 11 real records from 2026-09-05/06, then deploy (image 62 MB); after the startup sweep `ls /data/runs` shows the 11 real records and not the planted one; `/healthz` returned `retention_days: 30.0` with a `records_swept_at` timestamp. README "What is stored" and the listing FAQ now say 30 days.
- 2026-09-05 — Cover delivered: `surfaces/figma/listing/cover.png`, 1920 × 960 PNG (524,386 bytes; dimensions read from the file header), composited by the hub from `docs/exhibits/stage3-plugin/08-board-I-01-contested-zoom.png`, the I-01 section of run `20260905-221749-C-hosted` as the plugin drew it, cropped to the section and letterboxed on the FigJam canvas grey `#F5F5F5`. Screenshot-derived, not a canvas export: the boards had been deleted before an export was taken. It stands in for the spec's I-09 section, which exists only as the part-2 MCP-drawn exhibit. Cover blocker dropped; the privacy page and the plugin id at first publish remain.
- 2026-09-05 — Verdict screen at 440 px wrapped its six tiles 5 + 1 with "minutes" orphaned on its own row (user's screenshot, exhibit 22 when added). Fix in `ui.css`: the tile row is a flex-wrap with each tile at 30% basis, so six tiles always sit 3 + 3 and five sit 3 + 2 with the last two stretching; five tiles go on one row only when the panel is 420 px or wider (`.tiles[data-n="5"]`, the count stamped by `ui.ts`). Chosen over merging cost and time into one tile so both screens keep their own numbers readable. The scan script now records tiles per row and fails on an orphan, and gained a synthesis-result screen from the stored-result fixture. Re-run (`docs/exhibits/stage4-polish/harness-scan-tiles-rewrap.json`): verdict rows [3, 3] at 440 / 375 / 320; synthesis rows [5] at 440 and [3, 2] at 375 and 320; 0 layout issues, 0 orphans, on five screens.
- 2026-09-05 — Comparable missed by the research report: Blossom ("Bring user research to where the work happens", paid) appears in FigJam's plugin list under research. Before the listing goes live, check what it does and what it charges, and note where Motif's receipts-and-contested position differs. Open item for the submission session.
- 2026-09-06 — Motif submitted to Figma Community review at 00:31 CDT under the ETOT team profile @etot. Plugin id 1678295978273812914 (the first generated id was superseded; the second is the one in `surfaces/figma/manifest.json`, commit 7cd99fd). Listing link: pending, to be recorded on approval.
- 2026-09-06 — Category: the spec's "Research / Whiteboarding" does not exist in Figma's plugin taxonomy (the Whiteboarding entries are templates); chosen Design tools › Content generation.
- 2026-09-06 — Tags, capped at five by Figma: ux research, research synthesis, user interviews, transcripts, affinity mapping.
- 2026-09-06 — Images: thumbnail `surfaces/figma/listing/cover.png`; carousel `surfaces/figma/listing/carousel/01–05.png`, prepared by the hub from exhibits 11, 12, 04, 21, 14 by cropping to the plugin and canvas and letterboxing at 1920 × 960 on the canvas colour. The raw exhibits were nearly submitted with the menu bar, browser tabs, clock, and avatar visible; caught by the user before submission.
- 2026-09-06 — Data-security answers in the publish form: hosted backend receiving no plugin-API data; network requests only to motif-hosted.fly.dev; no user authentication; no plugin-API data stored; solo developer; no formal vulnerability process yet (GitHub issues / hello@etot.design); no accreditation.
- 2026-09-06 — Settings: comments on; support hello@etot.design; privacy https://etot.design/privacy/; 2FA required by Figma for publishing, and the Figma session had to be restarted after enabling it.
- 2026-09-06 — New rule, in `CONTRIBUTING.md` § Conventions and the plan's stage-4 QA gate: raw screenshots never leave the repo and, from now on, never enter it either. Every exhibit and every publication asset is cropped to the product surface and checked for personal information (menu bar, tabs, clock, avatar, file names) before it is saved; publication assets live under `surfaces/figma/listing/` or `docs/figures/`.
- 2026-09-06 — Listing link: https://www.figma.com/community/plugin/1678295978273812914 (the plugin id is the path; the page goes public on approval).
- 2026-09-06 — Blossom, the comparable the research report missed (FigJam's plugin list, research, marked paid). Read from tryblossom.io in a real browser on 2026-09-06: "the UXR data platform", tagline "Bring your research to where the work happens"; a video repository for research recordings (Zoom import, clips and highlight reels, synchronised meeting notes to tag key moments); the Figma/FigJam plugin (Community id 995608071385132676) drags playable video clips onto the canvas as video cards, slides, and stickies, adds video to presentations, and publishes via shareable links; it needs a Blossom account (the plugin has its own signup page). No public price: the site's Pricing link is a dead anchor to the homepage, no dollar amount appears anywhere, the only calls to action are "Start free trial" and "Request a demo", so pricing is sales-led and unverifiable from outside. Difference from Motif: Blossom brings recorded evidence (video) to the canvas for people to watch and tag; Motif produces claims from transcripts with verbatim receipts, a critic, and contested markings, and runs on the user's own key with no account. Adjacent, not overlapping; worth a sentence in the case study's comparables.
- 2026-09-06 — History scrub, before anything else: HEAD is `69ebee5e23e4da95964d3da789fb518e77922f3f` (main, clean, pushed). Reason: the 22 raw screenshots under `docs/exhibits/stage3-plugin/`, committed between `5aae6ba` and `0590f7e`, show the user's employer file names in the Figma tab strip on every exhibit and the last four characters of an API key on 02. Plan: mirror backup, `git filter-repo --path docs/exhibits/stage3-plugin/ --invert-paths` in a fresh clone, cropped replacements with the same 22 filenames committed on top, force-push by the user, working checkout re-cloned. This note's commit is the last pre-scrub commit.
- 2026-09-06 — History scrubbed. `git filter-repo --path docs/exhibits/stage3-plugin/ --invert-paths` in a fresh clone removed the folder from every commit (3 commits had touched it; 0 now; pack 43.47 MiB → 24.22 MiB, 822 → 796 objects before the replacements were added). Pre-scrub HEAD `1f7a29d` (the last pre-scrub commit, on top of `69ebee5`); mirror backup at `~/opt/claude/motif-mirror-backup-2026-09-06`, kept. The 22 cropped replacements from the hub carry the same filenames (01–22), so every notes line that cites an exhibit by number stays valid; each is 2814 × 1551, cropped to the plugin panel and canvas, checked on 01 and 02 (no tab strip, no menu bar, key fragment blanked). Reason: the raw screenshots showed the user's employer file names in the Figma tab strip on every exhibit and the last four characters of an API key on 02. filter-repo also rewrote the old hashes inside commit messages to their new equivalents (the pre-scrub-HEAD commit's subject now names the rewritten hash); this file's text was not rewritten.
- 2026-09-06 — Part 3 case-study copy drafted in the hub from `docs/part3-notes.md` into `docs/case-study/part3-draft.md`: nine sections on part 2's chassis shape, hero = the privacy test (`test_synthesize_job_end_to_end`), trace = the counter-receipt round trip (6 `quote_mismatch` → 0 mechanical failures). Nine brackets left rather than invented, listed at the top of the draft; the two that block publication are the Community review outcome and the first Fly.io invoice. The privacy test has no exhibit file, so the hero is drafted as a terminal block.
