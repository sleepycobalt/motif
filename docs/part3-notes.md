# Part 3 notes — Motif for Figma

Running list of moments worth writing up for the part-3 case study (the hosted engine, the plugin as product, tiers, first users, first revenue). One dated line per moment. Numbers and quotes are copied from the file named in the line, never recalled.

- 2026-09-04 — Part 3 opened. Read `docs/specs/motif-figma-plugin.md` (v2, the commercial surface), `docs/specs/motif-mcp-server.md`, and `CONTRIBUTING.md`. Proposal written to `docs/specs/part3-build-plan.md` before any code; awaiting ruling on hosting, Design-editor timing, and payment rails.
- 2026-09-04 — Engine constraint for BYOK: `core/llm.py` builds one module-level `Anthropic()` from the environment (`client()`), so the hosted service cannot use a per-user key without an engine change. Proposed: a `contextvars.ContextVar` for the client, set per job, defaulting to the environment so the CLI and MCP local mode are untouched.
- 2026-09-04 — Why a job API rather than one request per tool: from `docs/part2-notes.md`, the refactor regression run took 440.9 s and the recorded 15-transcript run took 2,064.6 s ($4.88). Submit / stream / fetch survives proxies and a closed plugin window; the draft contract in `surfaces/mcp/remote.py` (synchronous calls) will be replaced.
- 2026-09-04 — Privacy line in the spec ("processed for the run and discarded; nothing stored") maps onto the logger's existing `redact=True`: digests instead of prompt bodies, no corpus snapshot. The hosted service keeps the processed corpus in memory for the job plus a short TTL, then drops it. Consequence: remote `motif_receipts` only works within the TTL; the plugin gets receipts inline in the insights and never needs the endpoint.
- 2026-09-04 — Editor gap: stickies and connectors are FigJam-only plugin-API nodes; the Design editor has sections but no stickies. The board writer needs a second renderer (frames and text) for Design. Proposed order: FigJam first, Design at the end of stage 3.
- 2026-09-04 — Payments constraint: Figma's payments API sells one-time purchases or subscriptions, not a consumable balance, so prepaid credits need an ETOT-owned ledger topped up by Stripe Checkout via webhook. Matches the spec's Stripe fallback. To verify against Figma's docs when the paid tier is built.
- 2026-09-04 — Discipline miss found: the PyPI publish session (commit `927120c`) appended notes to `docs/part2-notes.md` but no three-line entry to `docs/log.md`. Added retroactively today, marked as such.
- 2026-09-04 — Listing asset gap: the icon source ("the field thumbnail, reduced" in the spec) is not in the repo; `find` for thumb/icon/field returned nothing outside `.venv`.
- 2026-09-04 — Ruling from the user: Fly.io; Design renderer at the end of stage 3; Stripe Checkout with an ETOT ledger, designed in stage 1 and built only when the prerequisites are dated; add a per-IP concurrency cap and a per-job transcript size cap on the BYOK path; the listing icon comes from the Motif field thumbnail (user supplies it); commit and push at the end of every session.
- 2026-09-04 — Stage 1 built offline: `core/llm.py` gained a per-context client (`using_key`) so a hosted job runs on its caller's key; `core/config.py` accepts a context key in place of the env var; `surfaces/hosted/` (app.py job API, jobs.py registry with caps and TTL, credits.py ledger, Dockerfile, fly.toml, README); `surfaces/mcp/remote.py` rewritten as a client of the job API and the MCP server's five remote branches wired to it; `tests/test_hosted.py` (11 tests: service through the test client, remote client on a real port, MCP server as a subprocess in remote mode). Suite: 30 passed in 4.19 s.
- 2026-09-04 — Surprise, and the most important finding of the session: the logger's `redact=True` digested the prompts but kept the model's response text verbatim in `calls/*.json`, and synthesis responses quote the transcripts (the receipts). The privacy claim in the plan ("redacted logs contain no transcript text") was false until a test asserted it: `test_synthesize_job_end_to_end` failed with "transcript text leaked into 003_synthesis.json". Fixed in `core/logger.py`: response text is now a digest too; `data` is dropped from the record; usage, timing, and stop reason stay. Write-up angle: a privacy property is a test, not a sentence in a README.
- 2026-09-04 — Second bug caught by the tests: the remote client merged the engine's result over the job id, so `run_id` in the tool result was the guessable run directory name instead of the random job id that later calls need. Fixed (`_as_tool_result`).
- 2026-09-04 — Environment surprise: `.venv/bin/pip` has a shebang pointing at `/Users/zzz/opt/claude/agentic-loop/synth-loop/.venv/bin/python3`, the venv's original path before the repo was renamed. `.venv/bin/python -m pip` works; the `pip` shim does not. Second time an "installed" environment has turned out to be a claim.
- 2026-09-04 — Corpus size finding for the upload caps: the 17 sample `.docx` files are 28 MB (Jamie.docx 5,879,730 bytes) because each embeds three ~1.7 MB Google Sans fonts (`unzip -l`), while `data/processed/*.txt` totals 620 KB. Caps set at 12 MB per file and 64 MB per job; stage 2 should weigh extracting paragraphs in the plugin before upload rather than sending 37 MB of base64.
- 2026-09-04 — MCP SDK 2.x gotcha (second of the day): `call_tool(progress_callback=...)` must be an async function; a plain lambda raises "object NoneType can't be used in 'await' expression" inside the SDK's dispatcher, and the error only shows in captured logs.
- 2026-09-04 — Deployment state: `flyctl` v0.4.99 installed via Homebrew; not authenticated ("no access token available"); `fly auth login` needs the user's browser. Region set to `ord` (the machine's timezone is America/Chicago). Local smoke: `motif-hosted` on port 8765 answered `/healthz` with `{"ok":true,"paid_tier":false,"jobs":0}`. Not yet done: deploy, and the stage-1 QA gate (MCP server in remote mode synthesising the sample corpus against the deployed service).
- 2026-09-04 — Follow-up flagged: PyPI's `etot-motif` 0.2.0 carries the old synchronous remote contract, so remote mode reaches installed users only with a 0.3.0 release after the deploy is verified.
- 2026-09-05 — Deploy gotchas, in order: `fly auth login` refuses to run under Claude Code's `!` prefix ("requires an interactive terminal"), so the user logged in from a separate terminal and the CLI's saved token was picked up; `fly apps create` refused until a card was on the account ("We need your payment information to continue"); `[build] dockerfile` in fly.toml is resolved relative to the toml's directory, not the repo root (first deploy failed looking for `surfaces/hosted/surfaces/hosted/Dockerfile`); the build context was 250 MB across 9,920 files (.venv 119 MB, runs 60 MB, .git 34 MB, data 32 MB, per the deploy warning) until a root `.dockerignore` allow-listed only pyproject, README, LICENSE, core, synth, surfaces. Final image 62 MB.
- 2026-09-05 — Live: app `motif-hosted`, region ord, machine 801e44c60de958, volume `motif_data` 1 GB, https://motif-hosted.fly.dev; `/healthz` returns `{"ok":true,"paid_tier":false,"jobs":0}`. Sizing: one shared-CPU machine, 1 GB memory, kept running. First hosting cost line for the case study once the first invoice arrives.
- 2026-09-05 — Stage 1 QA gate PASSED. The MCP server in remote mode (`MOTIF_REMOTE_URL=https://motif-hosted.fly.dev`, spawned by a client script) synthesised michelle + david through the hosted service. From `docs/exhibits/stage1-remote-qa/synthesize-result.json`: job `5GfWRXE3yUt43vfGyPhNSmzb`, hosted run `20260905-054758-C-hosted`, 11,482 words, 15 insights, contested I-02, I-05, I-08, 3 iterations, stop=max_iterations, $1.1409, engine wall 719.5 s, client wall 721.4 s, 21 progress notifications received by the host. From `run-record.json`: redacted=true, 8 calls, 224,535→73,279 tokens; final verdict failures I-08 missing_counterexample, I-02 unsupported, I-05 unsupported. `motif_receipts` by job id returned david:0022 found and nobody:0001 missing; `motif_board` returned 15 sections and 15 scripts; `motif_runs_get` returned the redacted record. Report copied to `docs/exhibits/stage1-remote-qa/report.md`.
- 2026-09-05 — Hosting overhead is 1.9 s on a 12-minute run (client wall 721.4 s against engine wall 719.5 s): the event stream kept pace and no proxy cut it. The run cost twice the local regression from `docs/part2-notes.md` ($0.5529, 1 iteration, 440.9 s) because this loop ran all 3 iterations, not because of hosting. From `progress.log`: synthesis 229.96 s; critic 86.32 s, 89.32 s, 107.38 s.
- 2026-09-05 — Privacy check on the volume itself (`fly ssh console`): `/data/runs/20260905-054758-C-hosted` is 556 KB with `calls`, `iterations`, `meta.json`, `notes.txt`, `output.json`, `output.md`; no `corpus/` directory anywhere under `/data/runs`; `calls/003_synthesis.json` holds `system`, `user`, and `text` as `{chars, sha256}` (user prompt 90,569 chars, response 16,447 chars). A five-word phrase from a cited turn appears only in `output.md` (the run's own report, receipts included) and in the scratch dir `/tmp/motif/motif-job-cxgr9cup/processed/`, both of which the expiry sweep deletes an hour after the job finishes (covered by `test_expiry_drops_corpus_and_run_content`; not re-observed live within this session).
- 2026-09-05 — Same notification finding as part 2: the QA client received 0 log notifications because it set no logging level, and 21 progress notifications because it passed a callback. The MCP SDK's deprecation warning on the logging capability (SEP-2577) prints on every emit in the server's stderr; harmless, noisy.
- 2026-09-05 — Found while wiring the QA client: in remote mode the MCP server never called `load_dotenv`, because only the config loader did and remote mode skips it, so a user's `.env` key would not have been forwarded. Fixed in `server.main()`; the QA run relied on the fix (no key was passed in the subprocess environment).
- 2026-09-05 — Next: 0.3.0 to PyPI so installed users get the working remote mode (0.2.0 has the old contract); then stage 2, the plugin on the free tier.
- 2026-09-05 — Release 0.3.0: version bumped in `pyproject.toml`, `surfaces/mcp/server.py` (MCPServer version), and `surfaces/mcp/server.json` (server and package); hosted app already reported 0.3.0. Built, `twine check` passed on both artifacts, uploaded with the token as `TWINE_PASSWORD` on the one command; https://pypi.org/project/etot-motif/0.3.0/. Pre-upload checks on the local wheel in an empty venv: `motif --help`, local stdio handshake reporting `motif 0.3.0` and five tools, and two zero-cost remote-mode probes against the live service (an empty corpus fails the hosted job at ingest and the tool relays "all 1 transcript file(s) were empty"; receipts on an unknown job relays the service's 404).
- 2026-09-05 — Registry: yesterday's `mcp-publisher` token had expired (a fresh `login github` device flow was needed; the token is short-lived, so every publish needs the click). `publish` from `surfaces/mcp/` reported "Server io.github.sleepycobalt/motif version 0.3.0"; the public API now lists both 0.2.0 (active, not latest) and 0.3.0 (active, latest), both pointing at their PyPI versions.
- 2026-09-05 — PyPI index race: `pip install etot-motif==0.3.0` run immediately after the upload failed with "from versions: 0.2.0" even though the registry had already validated 0.3.0 against PyPI's JSON API; the same command succeeded on the first retry about two minutes later. The simple index behind pip lags the JSON API by a minute or two.
- 2026-09-05 — Verified from PyPI in an empty venv (`etot-motif[mcp]==0.3.0`): `motif --help`; local handshake `motif 0.3.0`, five tools, verbatim error on a missing run; the two zero-cost remote probes pass. A live remote synthesis from the installed package (michelle + david, max_iterations=1) was then launched; result recorded in the next line.
- 2026-09-05 — Live remote synthesis from the PyPI-installed 0.3.0 PASSED (`docs/exhibits/stage1-remote-qa/pypi-0.3.0-live.json`): job `tvlHta4iJRZXnxHoxdYUzVPu`, hosted run `20260905-164234-C-hosted`, michelle + david, max_iterations=1, 13 insights, contested I-04 and I-11, $0.458, engine wall 338.3 s, client wall 339.2 s, 13 progress notifications; receipts by job id found david:0022; board 13 sections; run record redacted=true with 4 calls. Same shape as the part-2 local regression (`docs/part2-notes.md`: 13 insights, 4 calls, 440.9 s, $0.5529), so the hosted path costs nothing extra beyond 0.9 s of wall time on this run.
- 2026-09-05 — Stage 2 opened: the plugin on the free tier, no board. Ruling from the user: extract docx paragraphs in the plugin before upload; QA gate as planned (install to result in a fresh FigJam file under ten minutes; every screen checked at desktop and phone widths for clipped or overflowing elements before reporting). Environment: no Node on the machine (`brew install node`); `.venv/bin/pip` still broken (use `python -m pip`).
- 2026-09-05 — Service change for the plugin: the plugin UI runs in a sandboxed iframe whose origin is `null`, so every call is a CORS preflight. Added `CORSMiddleware` (any origin; GET/POST; headers Content-Type, X-Motif-Key, Authorization, Last-Event-ID) with a test, redeployed (image 70 MB). The key travels in a header, never a cookie, so an open origin policy exposes nothing the caller did not already hold. Verified live: `OPTIONS /v1/jobs` from `Origin: null` returns `access-control-allow-origin: *`.
- 2026-09-05 — Plugin built in `surfaces/figma/`: TypeScript, esbuild, no framework; `code.ts` (main thread: client storage for the key and the last job id, notifications, resize), `ui.ts` (screens, files, the run), `api.ts` (submit, follow SSE with resume, fetch), `docx.ts` (zip walk + `DecompressionStream("deflate-raw")` + `word/document.xml` paragraphs, no library), single-file `dist/ui.html` 33 KB, `dist/code.js` 2 KB. Manifest allow-lists only `https://motif-hosted.fly.dev`. The board writer is a disabled button labelled "next release".
- 2026-09-05 — docx parity, `surfaces/figma/test/docx_parity.mjs`: the plugin's extraction, run under Node with `@xmldom/xmldom`, on all 16 sample `.docx` (29.8 MB) produced 587 KB of text; ingesting that text and the original docx through `synth.ingest` gave equal manifests (15 transcripts, 101,042 words; one file skipped as empty on both paths) and, turn by turn, 1,868 turns compared, 1,867 byte-identical, 1 differing only by a line break inside a paragraph (python-docx keeps `\n` inside the turn; the plugin makes it a space, because the upload is one turn per line), 0 mismatches. The first run found this: Lisa (2) and Peter (1) had `<w:br/>` inside paragraphs, and a newline there would have split a turn in two on the text path.
- 2026-09-05 — Harness for the QA gate: `dist/ui.html` runs in a plain browser tab with `localStorage` standing in for Figma's client storage, `?files=` and `?demo=` hooks load transcripts and a saved result, and `test/harness.html` frames it at 440 (plugin default), 375 (phone), and 320 px. Chrome's window would not shrink below 1440 px on this machine (`resize_window` reported success, the viewport stayed 1440), so real iframes at those widths were the honest way to test; the UI treats a `?harness` frame as outside Figma.
- 2026-09-05 — Bug caught by the harness: the "A run is still going from last time" card showed with no run, because `.card { display: flex }` beat the browser's `[hidden]` rule (an author class rule outranks the UA stylesheet). Fix: `[hidden] { display: none !important; }`. It would have shown on every first open in Figma.
- 2026-09-05 — Every screen scanned at 440, 375, and 320 px (element rectangles against the viewport, `scrollWidth` against `clientWidth`, page horizontal scroll): key, setup with two docx transcripts extracted in the browser (Michelle 129 paragraphs / 5,644 words, David 76 / 6,155; server ingest then reported 128 and 74 turns, 11,482 words, identical to the docx path), running, result from the recorded 15-insight run (collapsed and with insights expanded), error with the rejected-key hint. Zero issues at any width; screenshots in `docs/exhibits/stage2-plugin/`.
- 2026-09-05 — The error screen relays the engine's message verbatim (Anthropic's 401 `API key is invalid.` for the placeholder key) and, above it, a plain hint for the three cases a user can act on: rejected key, no credit, engine unreachable. `last_job` is cleared on failure, so a failed run never offers itself for resumption.
- 2026-09-05 — Browser-path live run PASSED (`docs/exhibits/stage2-plugin/browser-run.json`, report in `browser-run-report.md`): launched from the plugin UI in the 440 px harness frame with the real key read into the page from the local `.env` (never through the chat), docx extracted client-side, job `ngqJoYXl17yhFKHVFdosoV84`, hosted run `20260905-171330-C-hosted`, 11,482 words, 12 insights, contested I-05 and I-11, 3 rounds, stop=max_iterations, $1.0328, engine wall 660.1 s, 20 progress events shown live in the running screen; the result screen rendered 12 insights with 2 contested badges and the tiles read "12 2 3 1.03 11.0"; `last_job` cleared on completion. Zero layout issues at 440 px on the running and result screens. This is the same corpus and settings as the part-2 recorded runs, so the plugin path costs what the CLI path costs.
- 2026-09-05 — Clipboard finding: in the harness both `navigator.clipboard.writeText` and `execCommand("copy")` failed on a synthetic click ("Document is not focused"), which is expected without user activation but would also be the outcome in any sandboxed iframe that denies clipboard access. Instead of a failed toast, the copy button now falls back to revealing the report in a selected, read-only box with a "press Cmd+C" note. Verified at 440 and 375 px: the box shows the full 58,847-character report with no overflow. Whether Figma's iframe grants clipboard-write on a real click is a question for the Figma run.
- 2026-09-05 — Tooling note: Chrome's `Page.captureScreenshot` timed out three times right after the report textarea was selected in two frames; navigation and JavaScript kept working. Screenshots for the copy-fallback state therefore come from the scan output, not an image.
- 2026-09-05 — Not done in this session, by construction: the ten-minute Figma run. Development plugins load only in the Figma desktop app, so the install-to-result timing and the clipboard check inside Figma's iframe are the user's run, from `surfaces/figma/README.md` ("Run it in Figma"). The plugin is not presented as done until that run is logged here.
