# Part 2 notes — Motif surfaces

Running list of moments worth writing up for the part-2 case study (MCP server, FigJam plugin). One dated line per moment. Numbers and quotes are copied from the file named in the line, never recalled.

- 2026-09-04 — Started the MCP-server build chat from `docs/specs/motif-mcp-server.md`. Step zero (verify Figma's MCP write path to FigJam on a real board) was put before any Motif code, on the reasoning that if a host can already write to FigJam through Figma's own MCP server, the MCP surface can ship before the FigJam plugin instead of depending on it.
- 2026-09-04 — Spec discrepancy: the spec's "Tools to expose" list has four tools (synthesize, critique, receipts, runs.get); the brief for this chat says five. Fifth is unconfirmed; proposed candidate is a board-output tool that depends on step zero. Source: `docs/specs/motif-mcp-server.md`.
- 2026-09-04 — Figma's write path exists and covers FigJam: the `use_figma` tool executes Plugin API JavaScript against the open file, with a `figma-use-figjam` skill for boards (stickies, sections, connectors, text, shapes, tables, code blocks). Writing requires the *remote* server (`https://mcp.figma.com/mcp`) and OAuth; the desktop server is read-only. Sources: developers.figma.com tools-and-prompts page; github.com/figma/mcp-server-guide skills `figma-use` and `figma-use-figjam`; help.figma.com guide (writes "currently available for free during the beta period", will become "usage-based paid").
- 2026-09-04 — Gotchas recorded from the `figma-use-figjam` skill: no `figma.createPage()` in FigJam (throws); `get_metadata` fails on FigJam files, use `get_figjam`; `console.log` is not returned, only `return` values; fonts must be loaded before any text mutation; every write script must return the created node IDs. `use_figma` targets the file whose `fileKey` is passed, and the file must be open.
- 2026-09-04 — Machine state at step zero: Figma desktop 126.8.16 running (from `pgrep -lf figma`); local MCP port 3845 refused (`curl` exit 000); remote endpoint answers 401 with `www-authenticate: Bearer ... authorization_uri="https://api.figma.com/.well-known/oauth-authorization-server"` and also lists `X-Figma-Token` as an allowed header — a personal access token may be an alternative to OAuth, untested. No Figma token on the machine (`.env` has only `ANTHROPIC_API_KEY`).
- 2026-09-04 — Registered the Figma remote MCP server in Claude Code local scope for this project (`claude mcp add --transport http figma https://mcp.figma.com/mcp`); status "Needs authentication". Step zero is blocked on the OAuth click, which only the account owner can do. Verification on a real board not yet run.
- 2026-09-04 — Engine read for the architecture: 1,337 lines across `core/` and `synth/` (from `wc -l`). Surprise: `synth/cli.py` shells out to `scripts/ingest.py` and `python -m synth.run` as subprocesses and finds the run directory by parsing stdout for `-> ... output.md`. The loop wiring lives inside `synth/run.py::main`, so there is no importable "engine service" yet — extracting one is the first build step, and it is the same service the FigJam plugin needs.
- 2026-09-04 — Hazard found: `core/logger.py::RunLogger` and `synth/run.py` print progress to stdout. An MCP server on stdio transport uses stdout for the protocol, so any print corrupts the stream. The logger needs an emit hook (stderr by default; MCP notifications in the server) before the server can exist.
- 2026-09-04 — Design gap in the spec: `motif.receipts(turn_ids)` has no corpus argument, but a turn id like `michelle:0042` only means something against a corpus. Proposed fix: runs snapshot their processed corpus into `runs/<id>/corpus/`, and receipts/critique take a `run_id`. The CLI currently ingests to a `mkdtemp` folder under `/tmp` that a run never records (`synth/cli.py`).
- 2026-09-04 — Tool naming: spec names use dots (`motif.runs.get`). Some MCP hosts restrict tool names to `[A-Za-z0-9_-]`; plan is to expose underscore names (`motif_runs_get`) and keep the dotted names in docs as logical names.
- 2026-09-04 — Step zero PASSED. After OAuth, created a throwaway FigJam board via Figma's `create_new_file` (`https://www.figma.com/board/GQRzxmNRE8MlIhEqh5UwXe`, on the pro-tier team; the starter team was skipped because it caps file counts). One `use_figma` call with skills `figma-use,figma-use-figjam` created a section, three stickies (claim / receipt / counter-evidence, colour-coded yellow / green / red) and a labelled connector, and returned node ids `1:2, 1:7, 1:11, 1:15, 1:19`. `get_figjam` read all five back inside the section; screenshot matches. Evidence: `docs/exhibits/step-zero/board.png`, `docs/exhibits/step-zero/readback.xml`.
- 2026-09-04 — Consequence: the MCP surface no longer depends on the FigJam plugin for board output. The host (Claude Code, Cursor) can write a Motif run onto a board through Figma's own MCP server; Motif only has to hand it a layout. Motif never needs Figma credentials.
- 2026-09-04 — Surprise: the sticky palette doc lists red as `#FFB8A8`, but the readback reports that sticky's colour as `CUSTOM` while yellow `#FFE299` and green `#B3EFBD` came back as palette colours (`docs/exhibits/step-zero/readback.xml`). The board tool should pin its palette to colours that round-trip, or accept custom fills.
- 2026-09-04 — Decisions from the user: the spec has five tools — synthesize, critique, receipts, board, runs_get — board being the fifth and dependent on step zero. Architecture approved: engine module, logger emit hook, corpus snapshot per run.
- 2026-09-04 — Acceptance requirement added for `motif_critique`: before it is called done, run the six blind reports in `eval2/blind/` (R1–R6) through it and record agreement with the human scores in `eval2/scoring.md` in these notes. The blind files are markdown; the critic takes insights JSON, so the test will feed each report's `runs/<id>/output.json` (mapping in `eval2/key.json`) and compare the critic's failures with the sheet's Unsupported counts and P-03 / P-05 trap marks.
- 2026-09-04 — Build session. Environment surprise first: `.venv` had lost the project's dependencies (no `anthropic`, no `python-docx`; `pip list` showed 24 packages) even though yesterday's runs used it. `pip install -e ".[mcp]"` restored it. Write it up as: an "installed" environment is a claim, not a fact — the install step belongs in the smoke test.
- 2026-09-04 — The MCP Python SDK is now 2.x (installed 2.1.1): `FastMCP` is renamed `MCPServer`, `Context` is injected as a typed parameter, sync tools run on worker threads, and the logging capability is deprecated (SEP-2577, 2026-07-28) though still delivered. Built against 2.x rather than pinning 1.x. Progress goes out as both log and progress notifications; when hosts drop log notifications, progress messages remain.
- 2026-09-04 — Engine extraction done: `synth/engine.py` (ingest, synthesize, critique, critique_document, receipts, corpus_for_run, load_run, board); `core/logger.py` got `emit` (stderr default), `redact`, and `snapshot_corpus`; `synth/ingest.py` replaces the script (which is now a shim). `synth/run.py` and `synth/cli.py` are thin surfaces. The ingest refactor was checked byte-for-byte: re-ingesting `data/raw/Dataset-2` produced 31 files identical to `data/processed` (`diff -rq`).
- 2026-09-04 — Offline tests with a stubbed model: 19 pass in under 2 s (`tests/`), including the MCP server spawned over stdio and called through the SDK client. The stub caught a real behaviour I had not written down: when synthesis returns nothing, the loop still calls revise once (with no insights) before stopping. Not changed; noted.
- 2026-09-04 — Live regression of the refactored engine (`runs/20260904-070551-C-mcp-refactor/`, from its meta.json and the run log): condition C, michelle + david, 11,482 words, 1 iteration, 4 calls, 440.9 s, $0.5529, 13 insights, critic failed I-03 (unsupported, merged_insights) and I-10 (overconfident), stop=max_iterations. Run dir now carries `corpus/` with the two transcripts and manifest.
- 2026-09-04 — SDK gotcha: a tool that raises a plain exception reaches the host as "Error executing tool motif_receipts" with the real message hidden. Failures must subclass the SDK's `ToolError` to arrive verbatim. Found by the stdio round-trip test, not by unit tests.
- 2026-09-04 — motif_board verified on the real board with a real run: R5's 15 insights (`runs/20260903-184148-C-v2-eval2`) laid out as 15 sections, 78 stickies, 12 connectors, executed by me as the host through Figma's `use_figma` in three batches of five parallel calls; every script returned its node ids; `get_figjam` read all of it back (`docs/exhibits/board-r5/readback-summary.json`, `board-full.png`, `section-I-01.png`). Palette round-trip: yellow, orange, white, pink, blue, violet all came back as palette colours; only step-zero's red came back CUSTOM. Tallest section was 984 px against a 1000 px grid slot; slot raised to 1200.
- 2026-09-04 — Design choice worth writing up: the board tool returns a layout and scripts and never touches Figma. The host holds the credentials and runs the scripts. Motif stays a pure function of the transcripts; the FigJam plugin becomes optional rather than a dependency.
- 2026-09-04 — motif_critique acceptance, both paths, through the MCP server (`eval2/critique-acceptance.json`; per-run verdicts under `runs/20260904-07*-critique*`). Eight critic calls, each 119–286 s, about $0.33 each (from the critique runs' meta.json). Path 1, JSON from the six eval2 runs with their intake notes. Path 2, `eval2/blind/R5.md` as plain markdown with no key and no run output: structured into 15 claims by the deterministic Motif-format parser, and again by the model structurer when forced — the two structurings were identical on ids, claims, evidence turn lists, counter-evidence lists, confidence, and carried the same 35 evidence quotes. Structuring passes.
- 2026-09-04 — Acceptance agreement with `eval2/scoring.md` (copied from `eval2/critique-acceptance.json` summary): unsupported 25 of 32 checked insights agree; P-03 (Penni's dissent missing from the quant-frame insight) 5 of 8; P-05 (positive support case missing from the guidance-gap insight) 2 of 8. Verdict agreement does NOT fully pass. Pattern: the critic never raised `missing_counterexample` on any report's guidance-gap insight (R1 I-09, R2 I-09, R3 I-13, R4 I-05, R6 I-05) and raised nothing else on them either; it missed the sheet's unsupported calls on R3 I-05/I-13 and R4 I-08; on R4 I-17 (counter cites the interviewer) it fired `quote_mismatch` rather than `unsupported`, a catch under a different rule that my comparison counts as disagreement; and on R5 I-10 it said unsupported in all three paths while the human said supported — the same objection the original R5 run's critic left on that insight. This is the critic's known dissent-detection gap (README "Known gaps"), measured, not a tool defect; the tool relays the critic faithfully. Tuning the critic changes the instrument and needs the eval re-run — user's call.
- 2026-09-04 — The markdown paths fired `quote_mismatch` on the eight R5 insights that carry counter-evidence, because Motif's own report format prints receipts for evidence only (`synth/report.py`). Predicted before the run; confirmed. Either the report should print counter receipts too, or the critic should distinguish "no receipt in the source" from "receipt does not match". Not changed today.
- 2026-09-04 — Notification finding: the acceptance client received 0 log lines because it never set a logging level; with `log_level="info"` the same server delivers them (probe: 2 of 2 ingest lines). Progress notifications were only sent for model-call lines; now every emit line goes out on both channels, and a probe with a progress callback receives them. Lesson for the README: hosts that ignore log notifications still get progress messages.
- 2026-09-04 — Ruling from the user on the acceptance result: accept the measured gap, record it, no instrument changes; dissent hunting belongs to the v3 eval. Recorded in `eval2/critique-acceptance.json` under "ruling", counts unchanged, with two notes: quote_mismatch-vs-unsupported on R4 I-17 is one citation failure caught by a different layer, and the R5 I-10 objection matches the original run's own contested flag.
- 2026-09-04 — Step 5 (distribution): `surfaces/mcp/README.md` now carries install snippets for Claude Code, Cursor, and Claude Desktop (Desktop needs absolute paths and cannot open files, so the report comes back in the reply); a skill wrapper `surfaces/mcp/skills/motif/SKILL.md` (copied into `.claude/skills/motif/` for this repo) that tells an agent to read `contested` first, verify receipts before quoting, and never present a synthesis made without Motif as a Motif result; `listing.md` with directory copy; and `server.json` for the official MCP registry, validated against the 2025-12-11 schema. Surprise: the registry caps `description` at 100 characters; the first draft was rejected as too long. The PyPI name `etot-motif` is free (pypi.org returned 404 for it), and publishing there is the prerequisite for the registry entry.
- 2026-09-04 — Recorded run for the case study: `motif_synthesize` on `data/raw/Dataset-2` (`runs/20260904-154633-C/`, from its meta.json and output.json: 23 insights, 3 iterations, stop=max_iterations, contested I-09/I-10/I-11, $4.88, 2,064.6 s), laid out on the board by `motif_board` + `use_figma` as 23 sections at origin (3800, 0) with I-18 moved to x=5832 to clear the 1912 px I-17; node map in `runs/20260904-154633-C/board_nodes.json` (copy at `docs/exhibits/recorded-run/board_nodes.json`), screenshots `docs/exhibits/recorded-run/board-overview.png`, `board-I-09-contested.png`. Then `motif_critique` on an uncited AI summary of that run: fail, 9 claims, 21 fails, 2 warns, every claim bad_citation + unsupported (`docs/exhibits/recorded-run/critique-verdict.json`).
- 2026-09-04 — v3 eval item: the critic cited an interviewer turn in its own evidence. In `runs/20260904-165114-critique-doc/output.json` the I-06 missing_counterexample failure lists `sam:0068`, and `motif_receipts` reports that turn's speaker as "Researcher 1", interviewer=true. The interviewer rule runs in code against the synthesis's citations only; the model-judged rules' `turns` lists are not passed through it. Fix candidate: run the critic's located turns through the same receipts check before returning the verdict.
- 2026-09-04 — v3 eval item: a document's opportunities are not evaluated as claims. The summary's "Key Opportunities" section (three bullets) was absorbed by the model structurer into the `opportunity` fields of the nine barrier claims (`runs/20260904-165114-critique-doc/output.json`, `source_format: model`), so nothing in it was checked for evidence, and the `vague_opportunity` rule only saw the redistributed text. Decide whether a document's recommendations section should be structured as its own claims or explicitly reported as unevaluated.
